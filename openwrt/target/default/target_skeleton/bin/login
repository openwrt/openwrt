#!/bin/sh
[ "$FAILSAFE" = "true" ] && exec /bin/ash --login

[ -f /etc/sysconf ] && . /etc/sysconf

if [ "$BR2_SYSCONF_TELNET_FAILSAFE_ONLY" = "y" ]; then
	echo "Login failed."
	exit 0
fi

exec /bin/ash --login
