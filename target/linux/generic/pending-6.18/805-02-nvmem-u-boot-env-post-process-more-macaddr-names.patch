--- a/drivers/nvmem/layouts/u-boot-env.c
+++ b/drivers/nvmem/layouts/u-boot-env.c
@@ -60,6 +60,8 @@ static int u_boot_env_parse_cells(struct
 {
 	char *data = buf + data_offset;
 	char *var, *value, *eq;
+	const char *mac_vars[] = {"ethaddr", "eth6addr", "wanaddr", "wlanaddr",
+				  "macaddr", "mac_addr", "wifiaddr", "athaddr"};
 
 	for (var = data;
 	     var < data + data_len && *var;
@@ -78,10 +80,13 @@ static int u_boot_env_parse_cells(struct
 		info.offset = data_offset + value - data;
 		info.bytes = strlen(value);
 		info.np = of_get_child_by_name(dev->of_node, info.name);
-		if (!strcmp(var, "ethaddr")) {
-			info.raw_len = strlen(value);
-			info.bytes = ETH_ALEN;
-			info.read_post_process = u_boot_env_read_post_process_ethaddr;
+		for (int i = 0; i < ARRAY_SIZE(mac_vars); i++) {
+			if (!strcmp(var, mac_vars[i])) {
+				info.raw_len = strlen(value);
+				info.bytes = ETH_ALEN;
+				info.read_post_process = u_boot_env_read_post_process_ethaddr;
+				break;
+			}
 		}
 
 		nvmem_add_one_cell(nvmem, &info);
