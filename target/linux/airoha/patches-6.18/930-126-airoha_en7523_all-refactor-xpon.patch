From b99568ac8a6571b52e48bc60b2bc849492d6d5a0 Mon Sep 17 00:00:00 2001
From: Matheus Sampaio Queiroga <srherobrine20@gmail.com>
Date: Mon, 20 Jul 2026 12:00:17 -0300
Subject: [PATCH 126/129] refactor xpon

Signed-off-by: Matheus Sampaio Queiroga <srherobrine20@gmail.com>
---
 drivers/net/ethernet/airoha/airoha_eth.c   |    2 +-
 drivers/net/ethernet/airoha/airoha_eth.h   |    1 +
 drivers/net/ethernet/airoha/airoha_ploam.c |    4 +-
 drivers/net/ethernet/airoha/airoha_xpon.c  | 1019 +++++++++-----------
 4 files changed, 455 insertions(+), 571 deletions(-)

diff --git a/drivers/net/ethernet/airoha/airoha_eth.c b/drivers/net/ethernet/airoha/airoha_eth.c
index 7bc69a8218e5..2de24f6cfb1e 100644
--- a/drivers/net/ethernet/airoha/airoha_eth.c
+++ b/drivers/net/ethernet/airoha/airoha_eth.c
@@ -3729,7 +3729,7 @@ airoha_ethtool_get_link_ksettings(struct net_device *netdev,
 
 	cmd->base.speed = state.speed;
 	cmd->base.duplex = state.duplex;
-	cmd->base.autoneg = state.autoneg;
+	cmd->base.autoneg = AUTONEG_DISABLE;
 	cmd->base.port = state.port;
 	cmd->base.phy_address = 0xff;
 
diff --git a/drivers/net/ethernet/airoha/airoha_eth.h b/drivers/net/ethernet/airoha/airoha_eth.h
index 3b83d6d3aad2..db6bae9f71c7 100644
--- a/drivers/net/ethernet/airoha/airoha_eth.h
+++ b/drivers/net/ethernet/airoha/airoha_eth.h
@@ -157,6 +157,7 @@ enum airoha_gdm_index {
 enum airoha_xpon_mode {
 	AIROHA_XPON_MODE_GPON,
 	AIROHA_XPON_MODE_EPON,
+	AIROHA_XPON_MODE_XGSPON,
 };
 
 enum {
diff --git a/drivers/net/ethernet/airoha/airoha_ploam.c b/drivers/net/ethernet/airoha/airoha_ploam.c
index 93b8486e3625..1af07320b3bc 100644
--- a/drivers/net/ethernet/airoha/airoha_ploam.c
+++ b/drivers/net/ethernet/airoha/airoha_ploam.c
@@ -599,8 +599,8 @@ struct ploam_priv *ploam_alloc(const struct ploam_ops *ops, void *hw_priv,
 	pp->state   = GPON_O1_INITIAL;
 	pp->onu_id  = PLOAM_ONU_UNASSIGNED;
 
-	memcpy(pp->sn,     sn,     8);
-	memcpy(pp->passwd, passwd, 10);
+	memcpy(pp->sn, sn, sizeof(pp->sn));
+	memcpy(pp->passwd, passwd, sizeof(pp->passwd));
 
 	return pp;
 }
diff --git a/drivers/net/ethernet/airoha/airoha_xpon.c b/drivers/net/ethernet/airoha/airoha_xpon.c
index 3a3ab2787a1b..58cd812406f2 100644
--- a/drivers/net/ethernet/airoha/airoha_xpon.c
+++ b/drivers/net/ethernet/airoha/airoha_xpon.c
@@ -55,10 +55,10 @@
 	from_timer(var, callback_timer, timer_fieldname)
 #endif
 
-#define EN7523_XPON_REGION_BASE         0x1fb60000
-#define EN7523_GPON_REG_OFFSET          0x00004000
-#define EN7523_EPON_REG_OFFSET          0x00006000
-#define EN7523_EPON_REG_BASE            0x1fb66000
+#define V1_XPON_REGION_SIZE		0x00010000
+#define GPON_REG_OFFSET			0x00004000
+#define XGSPON_REG_OFFSET		0x00005000
+#define EPON_REG_OFFSET			0x00006000
 
 #define EN7523_SCU_WAN_CONF             0x070
 #define EN7523_SCU_WAN_MODE_MASK        GENMASK(7, 0)
@@ -69,7 +69,6 @@ static const u8 airoha_default_vendor_id[4] = {'M', 'T', 'K', 'G'};
 
 struct airoha_xpon_match_data {
 	enum airoha_xpon_mode mode;
-	bool mode_from_dt;
 };
 
 static struct device *airoha_xpon_find_lddla(struct device *dev)
@@ -165,6 +164,11 @@ static void airoha_xpon_update_netdev_link(struct net_device *netdev,
 		state.rx_line_rate_bps = 1000000000ULL;
 		state.tx_line_rate_bps = 1000000000ULL;
 		break;
+	case AIROHA_XPON_MODE_XGSPON:
+		state.speed = SPEED_10000;
+		state.rx_line_rate_bps = 10000000000ULL;
+		state.tx_line_rate_bps = 10000000000ULL;
+		break;
 	default:
 		return;
 	}
@@ -172,30 +176,6 @@ static void airoha_xpon_update_netdev_link(struct net_device *netdev,
 	airoha_eth_xpon_update_link(netdev, &state);
 }
 
-static int airoha_xpon_get_mode(struct device *dev,
-				 const struct airoha_xpon_match_data *data,
-				 enum airoha_xpon_mode *mode)
-{
-	const char *name;
-
-	*mode = data->mode;
-	if (!data->mode_from_dt)
-		return 0;
-
-	if (of_property_read_string(dev->of_node, "airoha,pon-mode", &name))
-		return 0;
-
-	if (!strcmp(name, "gpon"))
-		*mode = AIROHA_XPON_MODE_GPON;
-	else if (!strcmp(name, "epon"))
-		*mode = AIROHA_XPON_MODE_EPON;
-	else
-		return dev_err_probe(dev, -EINVAL,
-				     "invalid airoha,pon-mode '%s'\n", name);
-
-	return 0;
-}
-
 static int airoha_xpon_find_gdm2(struct device *dev,
 				 struct device_node *eth_node,
 				 struct net_device **netdev)
@@ -350,13 +330,10 @@ static void airoha_xpon_phy_stop(struct device *dev, struct phy *phy,
  * Register offsets from the GPON register window at physical 0x1fb64000.
  *
  * The vendor driver maps the encompassing region at 0x1fb60000 and the
- * generated register structure reserves the first 0x4000 bytes.  Some DTs
- * instead expose the register window directly at 0x1fb64000.  Probe accepts
- * both layouts and sets priv->regs to the actual GPON register window.
+ * generated register structure reserves the first 0x4000 bytes.  GPON and
+ * EPON live at fixed offsets in that shared xPON MAC region.
  * -------------------------------------------------------------------- */
 
-#define EN7523_GPON_REG_BASE		0x1fb64000
-
 #define GPON_ONU_ID		0x000
 #define GPON_GBL_CFG		0x004
 #define GPON_INT_STATUS		0x008
@@ -634,12 +611,31 @@ static const char *gpon_state_name(enum gpon_state state)
 	}
 }
 
-struct gpon_priv {
+#define EPON_MAX_LLID		8
+
+/* LLID registration states */
+enum llid_state {
+	LLID_STATE_WAIT		= 0,
+	LLID_STATE_REGISTERING	= 1,
+	LLID_STATE_REGISTERED	= 2,
+};
+
+struct epon_llid {
+	enum llid_state	state;
+	bool		valid;
+	u16		value;
+};
+
+struct xpon_priv {
 	void __iomem		*base;
-	void __iomem		*regs;
+	void __iomem		*gpon_reg;
+	void __iomem		*epon_reg;
+	void __iomem		*xgspon_reg;
+	void __iomem		*epon_reset_reg;
 	struct device		*dev;
 	struct regmap		*scu;
 	struct phy		*phy;
+	enum airoha_xpon_mode	mode;
 	bool			phy_initialized;
 	bool			phy_powered;
 	struct device_node	*eth_node;
@@ -652,10 +648,13 @@ struct gpon_priv {
 	int			irq;
 	bool			irq_enabled;
 
+	struct epon_llid	llid[EPON_MAX_LLID];
+
 	struct ploam_priv	*ploam;
+	int			registered_llids;
 
-	u8			sn[8];
-	u8			passwd[10];
+	u8			hw_sn[8];
+	u8			hw_passwd[10];
 	struct omci_identity	identity;
 	u8			aes_key[16];
 
@@ -703,17 +702,17 @@ struct gpon_priv {
  * Register accessors
  * -------------------------------------------------------------------- */
 
-static inline u32 gpon_read(struct gpon_priv *priv, u32 reg)
+static inline u32 gpon_read(struct xpon_priv *priv, u32 reg)
 {
-	return readl(priv->regs + reg);
+	return readl(priv->gpon_reg + reg);
 }
 
-static inline void gpon_write(struct gpon_priv *priv, u32 reg, u32 val)
+static inline void gpon_write(struct xpon_priv *priv, u32 reg, u32 val)
 {
-	writel(val, priv->regs + reg);
+	writel(val, priv->gpon_reg + reg);
 }
 
-static void gpon_dump_activation_regs(struct gpon_priv *priv,
+static void gpon_dump_activation_regs(struct xpon_priv *priv,
 				      const char *reason)
 {
 	u32 phy_tx_frames = 0, phy_tx_bursts = 0;
@@ -746,17 +745,17 @@ static void gpon_dump_activation_regs(struct gpon_priv *priv,
 		 phy_tx_frames, phy_tx_bursts, phy_ret);
 }
 
-static inline void gpon_set_bits(struct gpon_priv *priv, u32 reg, u32 bits)
+static inline void gpon_set_bits(struct xpon_priv *priv, u32 reg, u32 bits)
 {
 	gpon_write(priv, reg, gpon_read(priv, reg) | bits);
 }
 
-static inline void gpon_clear_bits(struct gpon_priv *priv, u32 reg, u32 bits)
+static inline void gpon_clear_bits(struct xpon_priv *priv, u32 reg, u32 bits)
 {
 	gpon_write(priv, reg, gpon_read(priv, reg) & ~bits);
 }
 
-static inline void gpon_rmw(struct gpon_priv *priv, u32 reg, u32 mask, u32 val)
+static inline void gpon_rmw(struct xpon_priv *priv, u32 reg, u32 mask, u32 val)
 {
 	gpon_write(priv, reg, (gpon_read(priv, reg) & ~mask) | val);
 }
@@ -765,28 +764,28 @@ static inline void gpon_rmw(struct gpon_priv *priv, u32 reg, u32 mask, u32 val)
  * Low-level hardware helpers
  * -------------------------------------------------------------------- */
 
-static int gpon_wait_bits(struct gpon_priv *priv, u32 reg, u32 mask,
+static int gpon_wait_bits(struct xpon_priv *priv, u32 reg, u32 mask,
 			  unsigned int timeout_us)
 {
 	u32 val;
 
-	return readl_poll_timeout_atomic(priv->regs + reg, val, val & mask,
+	return readl_poll_timeout_atomic(priv->gpon_reg + reg, val, val & mask,
 					  1, timeout_us);
 }
 
-static int gpon_set_fe_mode(struct gpon_priv *priv)
+static int gpon_set_fe_mode(struct xpon_priv *priv)
 {
 	return airoha_xpon_set_fe_mode(priv->dev, priv->eth_node,
 				      AIROHA_XPON_MODE_GPON);
 }
 
-static int gpon_set_fe_datapath(struct gpon_priv *priv, bool enable)
+static int gpon_set_fe_datapath(struct xpon_priv *priv, bool enable)
 {
 	return airoha_xpon_set_fe_datapath(priv->dev, priv->eth_node,
 					  AIROHA_XPON_MODE_GPON, enable);
 }
 
-static int gpon_prepare_hardware(struct gpon_priv *priv)
+static int gpon_prepare_hardware(struct xpon_priv *priv)
 {
 	u32 mbi;
 	int ret;
@@ -858,7 +857,7 @@ static int gpon_prepare_hardware(struct gpon_priv *priv)
 	return 0;
 }
 
-static void gpon_reset_activation_context(struct gpon_priv *priv)
+static void gpon_reset_activation_context(struct xpon_priv *priv)
 {
 	u32 sn_cfg;
 
@@ -884,7 +883,7 @@ static void gpon_reset_activation_context(struct gpon_priv *priv)
 	priv->bit_delay = 0;
 }
 
-static int gpon_dev_init(struct gpon_priv *priv)
+static int gpon_dev_init(struct xpon_priv *priv)
 {
 	/*
 	 * Reset all activation state that can survive an optical or interface
@@ -907,7 +906,7 @@ static int gpon_dev_init(struct gpon_priv *priv)
 	return 0;
 }
 
-static int gpon_load_credentials(struct gpon_priv *priv)
+static int gpon_load_credentials(struct xpon_priv *priv)
 {
 	struct omci_identity *identity = &priv->identity;
 	int ret;
@@ -927,19 +926,19 @@ static int gpon_load_credentials(struct gpon_priv *priv)
 		identity->password_source = OMCI_CONFIG_SOURCE_DEFAULT;
 	}
 
-	memcpy(priv->sn, identity->serial_number, sizeof(priv->sn));
-	memcpy(priv->passwd, identity->password, sizeof(priv->passwd));
+	memcpy(priv->hw_sn, identity->serial_number, sizeof(priv->hw_sn));
+	memcpy(priv->hw_passwd, identity->password, sizeof(priv->hw_passwd));
 	dev_info(priv->dev,
 		 "loaded normalized GPON identity (serial source %u, password source %u)\n",
 		 identity->serial_source, identity->password_source);
 	return 0;
 }
 
-static void gpon_set_serial_number_regs(struct gpon_priv *priv)
+static void gpon_set_serial_number_regs(struct xpon_priv *priv)
 {
 	u32 random_delay, sn_cfg, sn_req_threshold, tx_power_mode;
-	u32 vendor = get_unaligned_be32(priv->sn);
-	u32 vs_sn = get_unaligned_be32(priv->sn + 4);
+	u32 vendor = get_unaligned_be32(priv->hw_sn);
+	u32 vs_sn = get_unaligned_be32(priv->hw_sn + 4);
 
 	/*
 	 * Match vendor gponDevSetSerialNumber(): sn[0] occupies bits 31:24
@@ -956,13 +955,13 @@ static void gpon_set_serial_number_regs(struct gpon_priv *priv)
 
 	dev_info(priv->dev,
 		 "GPON hardware SN response: serial=%8phN vendor=%#08x vs_sn=%#08x readback=%#08x/%#08x cfg=%#08x threshold=%u tx_power=%u random_delay=%u\n",
-		 priv->sn, vendor, vs_sn,
+		 priv->hw_sn, vendor, vs_sn,
 		 gpon_read(priv, GPON_VENDOR_ID),
 		 gpon_read(priv, GPON_VS_SN),
 		 sn_cfg, sn_req_threshold, tx_power_mode, random_delay);
 }
 
-static void gpon_load_aes_shadow(struct gpon_priv *priv, const u8 key[16],
+static void gpon_load_aes_shadow(struct xpon_priv *priv, const u8 key[16],
 				  u32 switch_superframe)
 {
 	int i;
@@ -975,7 +974,7 @@ static void gpon_load_aes_shadow(struct gpon_priv *priv, const u8 key[16],
 		   switch_superframe & AES_KEY_SWITCH_CNT_MASK);
 }
 
-static int __gpon_set_tcont_hw(struct gpon_priv *priv, unsigned int index,
+static int __gpon_set_tcont_hw(struct xpon_priv *priv, unsigned int index,
 			       u16 alloc_id, bool valid)
 {
 	if (index < 16) {
@@ -1015,7 +1014,7 @@ static int __gpon_set_tcont_hw(struct gpon_priv *priv, unsigned int index,
 	return -EINVAL;
 }
 
-static int gpon_config_tcont_hw(struct gpon_priv *priv, unsigned int index,
+static int gpon_config_tcont_hw(struct xpon_priv *priv, unsigned int index,
 				u16 alloc_id, bool valid)
 {
 	int ret;
@@ -1042,7 +1041,7 @@ static int gpon_config_tcont_hw(struct gpon_priv *priv, unsigned int index,
 	return ret;
 }
 
-static int gpon_set_tcont_hw(struct gpon_priv *priv, unsigned int index,
+static int gpon_set_tcont_hw(struct xpon_priv *priv, unsigned int index,
 			     u16 alloc_id, bool valid)
 {
 	int ret;
@@ -1064,7 +1063,7 @@ static int gpon_set_tcont_hw(struct gpon_priv *priv, unsigned int index,
 	return ret;
 }
 
-static int gpon_find_tcont_alloc_locked(struct gpon_priv *priv, u16 alloc_id)
+static int gpon_find_tcont_alloc_locked(struct xpon_priv *priv, u16 alloc_id)
 {
 	int index;
 
@@ -1075,7 +1074,7 @@ static int gpon_find_tcont_alloc_locked(struct gpon_priv *priv, u16 alloc_id)
 	return -ENOENT;
 }
 
-static int gpon_find_tcont_entity_locked(struct gpon_priv *priv, u16 entity_id)
+static int gpon_find_tcont_entity_locked(struct xpon_priv *priv, u16 entity_id)
 {
 	int index;
 
@@ -1086,7 +1085,7 @@ static int gpon_find_tcont_entity_locked(struct gpon_priv *priv, u16 entity_id)
 	return -ENOENT;
 }
 
-static int gpon_find_free_tcont_locked(struct gpon_priv *priv)
+static int gpon_find_free_tcont_locked(struct xpon_priv *priv)
 {
 	int index;
 
@@ -1097,7 +1096,7 @@ static int gpon_find_free_tcont_locked(struct gpon_priv *priv)
 	return -ENOSPC;
 }
 
-static int gpon_tcont_entity_to_index(struct gpon_priv *priv, u16 entity_id,
+static int gpon_tcont_entity_to_index(struct xpon_priv *priv, u16 entity_id,
 				      unsigned int *index)
 {
 	int found;
@@ -1111,7 +1110,7 @@ static int gpon_tcont_entity_to_index(struct gpon_priv *priv, u16 entity_id,
 	return found < 0 ? found : 0;
 }
 
-static int gpon_read_gem_port_hw(struct gpon_priv *priv, u16 gem_port_id,
+static int gpon_read_gem_port_hw(struct xpon_priv *priv, u16 gem_port_id,
 				 bool *valid, bool *encrypted)
 {
 	u32 status;
@@ -1132,7 +1131,7 @@ static int gpon_read_gem_port_hw(struct gpon_priv *priv, u16 gem_port_id,
 	return 0;
 }
 
-static int gpon_set_gem_port_hw(struct gpon_priv *priv, u16 gem_port_id,
+static int gpon_set_gem_port_hw(struct xpon_priv *priv, u16 gem_port_id,
 				bool valid, bool encrypted)
 {
 	bool read_encrypted, read_valid;
@@ -1175,7 +1174,7 @@ static int gpon_set_gem_port_hw(struct gpon_priv *priv, u16 gem_port_id,
 	return ret ?: -EIO;
 }
 
-static int gpon_set_alloc_id_hw(struct gpon_priv *priv, u16 alloc_id,
+static int gpon_set_alloc_id_hw(struct xpon_priv *priv, u16 alloc_id,
 				bool allocate)
 {
 	int index, ret = 0;
@@ -1214,7 +1213,7 @@ static int gpon_set_alloc_id_hw(struct gpon_priv *priv, u16 alloc_id,
 	return ret;
 }
 
-static int gpon_set_omci_tcont_hw(struct gpon_priv *priv, u16 entity_id,
+static int gpon_set_omci_tcont_hw(struct xpon_priv *priv, u16 entity_id,
 				  u16 alloc_id, bool valid,
 				  unsigned int *channel)
 {
@@ -1268,7 +1267,7 @@ static int gpon_set_omci_tcont_hw(struct gpon_priv *priv, u16 entity_id,
  * Hardware-side PLOAM FIFO access
  * -------------------------------------------------------------------- */
 
-static inline u32 gpon_ploam_read_word(struct gpon_priv *priv)
+static inline u32 gpon_ploam_read_word(struct xpon_priv *priv)
 {
 	/*
 	 * The GPON FIFO exposes each PLOAM word directly in protocol order:
@@ -1279,18 +1278,18 @@ static inline u32 gpon_ploam_read_word(struct gpon_priv *priv)
 	return gpon_read(priv, GPON_PLOAMd_RDATA);
 }
 
-static inline void gpon_ploam_write_word(struct gpon_priv *priv, u32 val)
+static inline void gpon_ploam_write_word(struct xpon_priv *priv, u32 val)
 {
 	/* Keep the same register/protocol ordering for upstream messages. */
 	gpon_write(priv, GPON_PLOAMu_WDATA, val);
 }
 
-static int gpon_wait_ploam_tx_space(struct gpon_priv *priv, u32 *available)
+static int gpon_wait_ploam_tx_space(struct xpon_priv *priv, u32 *available)
 {
 	u32 status;
 	int ret;
 
-	ret = readl_poll_timeout_atomic(priv->regs + GPON_PLOAMu_FIFO_STS,
+	ret = readl_poll_timeout_atomic(priv->gpon_reg + GPON_PLOAMu_FIFO_STS,
 					status,
 					(status & PLOAMu_FIFO_AVAIL_MASK) >=
 					PLOAM_WORDS, 1,
@@ -1300,7 +1299,7 @@ static int gpon_wait_ploam_tx_space(struct gpon_priv *priv, u32 *available)
 	return ret;
 }
 
-static void gpon_hw_send_ploam(struct gpon_priv *priv,
+static void gpon_hw_send_ploam(struct xpon_priv *priv,
 			       const struct ploam_msg *msg, int times)
 {
 	u8 onu_id = msg->value[0] >> 24;
@@ -1329,13 +1328,13 @@ static void gpon_hw_send_ploam(struct gpon_priv *priv,
 	}
 }
 
-static void gpon_ploam_rx_queue_reset(struct gpon_priv *priv)
+static void gpon_ploam_rx_queue_reset(struct xpon_priv *priv)
 {
 	WRITE_ONCE(priv->ploam_rx_head, 0);
 	WRITE_ONCE(priv->ploam_rx_tail, 0);
 }
 
-static bool gpon_ploam_rx_queue_push(struct gpon_priv *priv,
+static bool gpon_ploam_rx_queue_push(struct xpon_priv *priv,
 				     const struct ploam_msg *msg)
 {
 	u16 head = READ_ONCE(priv->ploam_rx_head);
@@ -1355,7 +1354,7 @@ static bool gpon_ploam_rx_queue_push(struct gpon_priv *priv,
 	return true;
 }
 
-static bool gpon_ploam_rx_queue_pop(struct gpon_priv *priv,
+static bool gpon_ploam_rx_queue_pop(struct xpon_priv *priv,
 				    struct ploam_msg *msg)
 {
 	u16 tail = READ_ONCE(priv->ploam_rx_tail);
@@ -1372,7 +1371,7 @@ static bool gpon_ploam_rx_queue_pop(struct gpon_priv *priv,
 	return true;
 }
 
-static void gpon_fastpath_assign_onu_id(struct gpon_priv *priv,
+static void gpon_fastpath_assign_onu_id(struct xpon_priv *priv,
 					const struct ploam_msg *msg)
 {
 	u8 msg_sn[8];
@@ -1394,7 +1393,7 @@ static void gpon_fastpath_assign_onu_id(struct gpon_priv *priv,
 	msg_sn[5] = msg->value[2] >> 24;
 	msg_sn[6] = msg->value[2] >> 16;
 	msg_sn[7] = msg->value[2] >> 8;
-	if (memcmp(msg_sn, priv->sn, sizeof(msg_sn)))
+	if (memcmp(msg_sn, priv->hw_sn, sizeof(msg_sn)))
 		return;
 
 	onu_id = (msg->value[0] >> 8) & ONU_ID_MASK;
@@ -1411,7 +1410,7 @@ static void gpon_fastpath_assign_onu_id(struct gpon_priv *priv,
 	priv->assign_onu_fastpath++;
 }
 
-static void gpon_drain_ploam_fifo_irq(struct gpon_priv *priv)
+static void gpon_drain_ploam_fifo_irq(struct xpon_priv *priv)
 {
 	int budget = GPON_PLOAM_RX_QUEUE_LEN - 1;
 
@@ -1433,7 +1432,7 @@ static void gpon_drain_ploam_fifo_irq(struct gpon_priv *priv)
 	}
 }
 
-static void gpon_process_ploam_queue(struct gpon_priv *priv)
+static void gpon_process_ploam_queue(struct xpon_priv *priv)
 {
 	struct ploam_msg msg;
 
@@ -1458,7 +1457,7 @@ static void gpon_cb_send_upstream(void *hw_priv, const struct ploam_msg *msg,
 
 static void gpon_cb_set_onu_id(void *hw_priv, u8 onu_id)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 
 	dev_info(priv->dev, "GPON assigned ONU-ID %u\n", onu_id);
 	gpon_write(priv, GPON_ONU_ID, ONU_ID_VLD | (onu_id & ONU_ID_MASK));
@@ -1468,7 +1467,7 @@ static void gpon_cb_set_onu_id(void *hw_priv, u8 onu_id)
 
 static void gpon_cb_set_eqd_o4(void *hw_priv, u32 byte_delay, u32 bit_delay)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 
 	dev_info(priv->dev, "GPON O4 EqD: byte_delay=%u bit_delay=%u\n",
 		 byte_delay, bit_delay);
@@ -1481,7 +1480,7 @@ static void gpon_cb_set_eqd_o4(void *hw_priv, u32 byte_delay, u32 bit_delay)
 
 static void gpon_cb_adjust_eqd_o5(void *hw_priv, u32 new_eqd)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	u32 curr_eqd = ploam_get_eqd(priv->ploam);
 	int delta = (int)new_eqd - (int)curr_eqd;
 	u32 sync_raw;
@@ -1527,7 +1526,7 @@ static void gpon_cb_adjust_eqd_o5(void *hw_priv, u32 new_eqd)
 
 static void gpon_cb_enable_us_fec(void *hw_priv)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 
 	dev_info(priv->dev, "enabling GPON upstream FEC\n");
 	gpon_set_bits(priv, GPON_GBL_CFG, GBL_CFG_US_FEC_EN);
@@ -1538,7 +1537,7 @@ static void gpon_cb_set_overhead(void *hw_priv,
 				  u8 t3_pbits, const u8 delim[3],
 				  bool delay_mode, u16 delay_time)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	u32 prmbl, pre_dly;
 	int ret;
 
@@ -1588,7 +1587,7 @@ static void gpon_cb_set_overhead(void *hw_priv,
 
 static void gpon_cb_set_t3_preamble(void *hw_priv, u8 o3_t3, u8 o5_t3)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	u32 val;
 	int ret;
 
@@ -1616,7 +1615,7 @@ static void gpon_cb_set_t3_preamble(void *hw_priv, u8 o3_t3, u8 o5_t3)
 
 static void gpon_cb_set_key_switch_time(void *hw_priv, u32 superframe)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 
 	dev_info(priv->dev, "GPON AES key switch superframe=%u\n",
 		 superframe & AES_KEY_SWITCH_CNT_MASK);
@@ -1625,7 +1624,7 @@ static void gpon_cb_set_key_switch_time(void *hw_priv, u32 superframe)
 
 static void gpon_cb_request_new_key(void *hw_priv)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 
 	dev_info(priv->dev, "GPON generating a new AES key\n");
 	get_random_bytes(priv->aes_key, 16);
@@ -1637,7 +1636,7 @@ static void gpon_cb_request_new_key(void *hw_priv)
 
 static void gpon_cb_set_ber_interval(void *hw_priv, u32 interval_ms)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 
 	dev_info(priv->dev, "GPON BER reporting interval=%u ms\n",
 		 interval_ms);
@@ -1651,7 +1650,7 @@ static void gpon_cb_set_ber_interval(void *hw_priv, u32 interval_ms)
 
 static int gpon_cb_set_omci_gem(void *hw_priv, u16 gem_port_id, bool valid)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	u8 onu_id = ploam_get_onu_id(priv->ploam);
 	u32 reg_val;
 	int ret, tcont_ret;
@@ -1725,7 +1724,7 @@ static int gpon_cb_set_omci_gem(void *hw_priv, u16 gem_port_id, bool valid)
 static void gpon_cb_set_gem_encryption(void *hw_priv, u16 port_id,
 					u8 encrypt_mode)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	int ret;
 
 	ret = gpon_set_gem_port_hw(priv, port_id, true, encrypt_mode == 3);
@@ -1741,7 +1740,7 @@ static void gpon_cb_set_gem_encryption(void *hw_priv, u16 port_id,
 
 static void gpon_cb_set_alloc_id(void *hw_priv, u16 alloc_id, bool allocate)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	int ret;
 
 	dev_info(priv->dev, "GPON Alloc-ID %s: %u\n",
@@ -1755,7 +1754,7 @@ static void gpon_cb_set_alloc_id(void *hw_priv, u16 alloc_id, bool allocate)
 int airoha_gpon_omci_hw_set_tcont(void *hw_priv, u16 entity_id,
 				  u16 alloc_id, bool valid)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	unsigned int channel = 0;
 	int ret;
 
@@ -1780,7 +1779,7 @@ int airoha_gpon_omci_hw_set_gem_port(void *hw_priv, u16 entity_id,
 				     u16 gem_port_id, u16 tcont_entity_id,
 				     u8 direction, bool valid, bool encrypted)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	struct airoha_xpon_service_cfg service = {
 		.gem_port_id = gem_port_id,
 		.queue = 0,
@@ -1820,7 +1819,7 @@ int airoha_gpon_omci_hw_set_gem_port(void *hw_priv, u16 entity_id,
 
 int airoha_gpon_omci_hw_set_uni(void *hw_priv, u16 entity_id, bool enable)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 
 	dev_dbg(priv->dev, "OMCI UNI %#x requested %s\n", entity_id,
 		enable ? "enabled" : "disabled");
@@ -1831,7 +1830,7 @@ int airoha_gpon_omci_hw_get_telemetry(void *hw_priv,
 				      struct omci_telemetry *telemetry)
 {
 	struct airoha_lddla_telemetry optical = {};
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	bool downstream_fec, upstream_fec;
 	int ret;
 
@@ -1887,11 +1886,11 @@ int airoha_gpon_omci_hw_get_telemetry(void *hw_priv,
 }
 
 /* Forward declaration needed by gpon_disable */
-static void gpon_disable(struct gpon_priv *priv);
+static void gpon_disable(struct xpon_priv *priv);
 
 static void gpon_cb_state_changed(void *hw_priv, enum gpon_state state)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 	enum airoha_xpon_phy_gpon_oper_state phy_state;
 	bool update_phy_state = true;
 	int ret;
@@ -1987,7 +1986,7 @@ static void gpon_cb_state_changed(void *hw_priv, enum gpon_state state)
 
 static void gpon_cb_deactivate(void *hw_priv)
 {
-	struct gpon_priv *priv = hw_priv;
+	struct xpon_priv *priv = hw_priv;
 
 	/*
 	 * The callback runs while the ordered IRQ worker is processing the
@@ -2020,7 +2019,7 @@ static const struct ploam_ops gpon_ploam_ops = {
  * Enable / disable
  * -------------------------------------------------------------------- */
 
-static int gpon_enable(struct gpon_priv *priv)
+static int gpon_enable(struct xpon_priv *priv)
 {
 	u32 fifo_depth, known, pending, unknown;
 	int ret;
@@ -2105,7 +2104,7 @@ static int gpon_enable(struct gpon_priv *priv)
 	return 0;
 }
 
-static void gpon_disable(struct gpon_priv *priv)
+static void gpon_disable(struct xpon_priv *priv)
 {
 	int ret;
 
@@ -2190,7 +2189,7 @@ static void gpon_disable(struct gpon_priv *priv)
 
 static void gpon_ber_timer_fn(struct timer_list *t)
 {
-	struct gpon_priv *priv = timer_container_of(priv, t, ber_timer);
+	struct xpon_priv *priv = timer_container_of(priv, t, ber_timer);
 	u32 depth;
 
 	/* Report a missing MAC IRQ while retaining the FIFO contents for debug. */
@@ -2209,8 +2208,8 @@ static void gpon_ber_timer_fn(struct timer_list *t)
 /* TO1: O3/O4 timeout — no Ranging_Time received within 10 s → return to O2 */
 static void gpon_to1_work_fn(struct work_struct *work)
 {
-	struct gpon_priv *priv =
-		container_of(to_delayed_work(work), struct gpon_priv, to1_work);
+	struct xpon_priv *priv =
+		container_of(to_delayed_work(work), struct xpon_priv, to1_work);
 	enum gpon_state st = ploam_get_state(priv->ploam);
 
 	/*
@@ -2231,8 +2230,8 @@ static void gpon_to1_work_fn(struct work_struct *work)
 /* TO2: O6 timeout — no Popup received within 100 ms → full disable */
 static void gpon_to2_work_fn(struct work_struct *work)
 {
-	struct gpon_priv *priv =
-		container_of(to_delayed_work(work), struct gpon_priv, to2_work);
+	struct xpon_priv *priv =
+		container_of(to_delayed_work(work), struct xpon_priv, to2_work);
 
 	if (ploam_get_state(priv->ploam) == GPON_O6_POPUP) {
 		dev_warn(priv->dev, "GPON TO2 expired in O6, resetting\n");
@@ -2242,8 +2241,8 @@ static void gpon_to2_work_fn(struct work_struct *work)
 
 static void gpon_restart_work_fn(struct work_struct *work)
 {
-	struct gpon_priv *priv =
-		container_of(to_delayed_work(work), struct gpon_priv,
+	struct xpon_priv *priv =
+		container_of(to_delayed_work(work), struct xpon_priv,
 			     restart_work);
 	int ret;
 
@@ -2280,7 +2279,7 @@ static void gpon_restart_work_fn(struct work_struct *work)
  * Interrupt handler
  * -------------------------------------------------------------------- */
 
-static void gpon_dump_error_counters(struct gpon_priv *priv, u32 errors)
+static void gpon_dump_error_counters(struct xpon_priv *priv, u32 errors)
 {
 	dev_warn_ratelimited(priv->dev,
 			     "GPON error: irq=%#x sfifo=%#x bfifo=%#x ctrl=%#x\n",
@@ -2301,8 +2300,8 @@ static void gpon_dump_error_counters(struct gpon_priv *priv, u32 errors)
 
 static void gpon_irq_work_fn(struct work_struct *work)
 {
-	struct gpon_priv *priv =
-		container_of(work, struct gpon_priv, irq_work);
+	struct xpon_priv *priv =
+		container_of(work, struct xpon_priv, irq_work);
 	u32 active;
 
 	/*
@@ -2380,7 +2379,7 @@ static void gpon_irq_work_fn(struct work_struct *work)
 
 static irqreturn_t gpon_isr(int irq, void *data)
 {
-	struct gpon_priv *priv = data;
+	struct xpon_priv *priv = data;
 	u32 active, enabled, raw;
 
 	raw = gpon_read(priv, GPON_INT_STATUS);
@@ -2410,14 +2409,14 @@ static irqreturn_t gpon_isr(int irq, void *data)
 
 static void gpon_sfp_attach(void *upstream, struct sfp_bus *bus)
 {
-	struct gpon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "GPON SFP bus attached\n");
 }
 
 static void gpon_sfp_detach(void *upstream, struct sfp_bus *bus)
 {
-	struct gpon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "GPON SFP bus detached\n");
 }
@@ -2425,7 +2424,7 @@ static void gpon_sfp_detach(void *upstream, struct sfp_bus *bus)
 static int gpon_sfp_module_insert(void *upstream,
 				   const struct sfp_eeprom_id *id)
 {
-	struct gpon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "GPON SFP module inserted\n");
 	return 0;
@@ -2433,7 +2432,7 @@ static int gpon_sfp_module_insert(void *upstream,
 
 static void gpon_sfp_module_remove(void *upstream)
 {
-	struct gpon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "GPON SFP module removed\n");
 	cancel_delayed_work_sync(&priv->restart_work);
@@ -2443,7 +2442,7 @@ static void gpon_sfp_module_remove(void *upstream)
 
 static int gpon_sfp_module_start(void *upstream)
 {
-	struct gpon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 	enum gpon_state state;
 
 	state = ploam_get_state(priv->ploam);
@@ -2466,7 +2465,7 @@ static int gpon_sfp_module_start(void *upstream)
 
 static void gpon_sfp_module_stop(void *upstream)
 {
-	struct gpon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "GPON SFP module stop\n");
 	cancel_delayed_work_sync(&priv->restart_work);
@@ -2475,7 +2474,7 @@ static void gpon_sfp_module_stop(void *upstream)
 
 static void gpon_sfp_link_down(void *upstream)
 {
-	struct gpon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_warn(priv->dev, "GPON optical link down / LOS\n");
 	ploam_notify_los(priv->ploam);
@@ -2483,7 +2482,7 @@ static void gpon_sfp_link_down(void *upstream)
 
 static void gpon_sfp_link_up(void *upstream)
 {
-	struct gpon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "GPON optical link up\n");
 }
@@ -2505,7 +2504,7 @@ static const struct sfp_upstream_ops gpon_sfp_ops = {
 
 static int gpon_link_start(void *data)
 {
-	struct gpon_priv *priv = data;
+	struct xpon_priv *priv = data;
 
 	if (READ_ONCE(priv->started))
 		return 0;
@@ -2521,7 +2520,7 @@ static int gpon_link_start(void *data)
 
 static void gpon_link_stop(void *data)
 {
-	struct gpon_priv *priv = data;
+	struct xpon_priv *priv = data;
 
 	if (!READ_ONCE(priv->started))
 		return;
@@ -2539,247 +2538,6 @@ static const struct airoha_xpon_link_ops gpon_link_ops = {
 	.stop = gpon_link_stop,
 };
 
-/* -----------------------------------------------------------------------
- * Platform driver
- * -------------------------------------------------------------------- */
-
-static int gpon_probe(struct platform_device *pdev)
-{
-	struct device *dev = &pdev->dev;
-	struct gpon_priv *priv;
-	struct resource *res;
-	u32 reg_offset;
-	int ret;
-
-	dev_info(dev, "probing GPON MAC\n");
-	priv = devm_kzalloc(dev, sizeof(*priv), GFP_KERNEL);
-	if (!priv)
-		return -ENOMEM;
-	priv->dev = dev;
-
-	priv->scu = syscon_regmap_lookup_by_phandle(dev->of_node,
-					    "airoha,scu");
-	if (IS_ERR(priv->scu)) {
-		ret = dev_err_probe(dev, PTR_ERR(priv->scu),
-				    "failed to get SCU regmap\n");
-		goto err_put_eth_node;
-	}
-
-	priv->phy = devm_phy_get(dev, "xpon");
-	if (IS_ERR(priv->phy)) {
-		ret = dev_err_probe(dev, PTR_ERR(priv->phy),
-				    "failed to get digital xPON PHY\n");
-		goto err_put_eth_node;
-	}
-
-	priv->eth_node = of_parse_phandle(dev->of_node, "ethernet", 0);
-	if (!priv->eth_node) {
-		ret = dev_err_probe(dev, -EINVAL,
-				    "missing Ethernet datapath phandle\n");
-		goto err_put_eth_node;
-	}
-	dev_info(dev, "GPON datapath phandle: %pOF\n", priv->eth_node);
-
-	ret = airoha_xpon_find_gdm2(dev, priv->eth_node, &priv->gdm_dev);
-	if (ret)
-		goto err_put_eth_node;
-
-	res = platform_get_resource_byname(pdev, IORESOURCE_MEM, "mac");
-	if (!res)
-		res = platform_get_resource(pdev, IORESOURCE_MEM, 0);
-	if (!res) {
-		ret = dev_err_probe(dev, -EINVAL, "missing mac resource\n");
-		goto err_put_eth_node;
-	}
-
-	priv->base = devm_ioremap_resource(dev, res);
-	if (IS_ERR(priv->base)) {
-		ret = dev_err_probe(dev, PTR_ERR(priv->base), "needs xPON mac base");
-		goto err_put_eth_node;
-	}
-
-	if (res->start == EN7523_XPON_REGION_BASE) {
-		if (resource_size(res) < EN7523_GPON_REG_OFFSET +
-					 GPON_SLEEP_CNT + sizeof(u32)) {
-			ret = dev_err_probe(dev, -EINVAL,
-				"GPON region is too small: %pR\n", res);
-			goto err_put_eth_node;
-		}
-		reg_offset = EN7523_GPON_REG_OFFSET;
-	} else if (res->start == EN7523_GPON_REG_BASE) {
-		if (resource_size(res) < GPON_SLEEP_CNT + sizeof(u32)) {
-			ret = dev_err_probe(dev, -EINVAL,
-				"GPON register window is too small: %pR\n", res);
-			goto err_put_eth_node;
-		}
-		reg_offset = 0;
-	} else {
-		ret = dev_err_probe(dev, -EINVAL,
-			"unsupported GPON MAC resource: %pR\n", res);
-		goto err_put_eth_node;
-	}
-
-	priv->regs = priv->base + reg_offset;
-	dev_info(dev, "GPON resource %pR, register offset %#x\n",
-		 res, reg_offset);
-
-	priv->fsm_wq = alloc_ordered_workqueue("%s-gpon-fsm",
-					       WQ_MEM_RECLAIM, dev_name(dev));
-	if (!priv->fsm_wq) {
-		ret = -ENOMEM;
-		goto err_put_eth_node;
-	}
-
-	mutex_init(&priv->tcont_lock);
-	memset(priv->tcont_alloc_id, 0xff, sizeof(priv->tcont_alloc_id));
-	memset(priv->tcont_entity_id, 0xff, sizeof(priv->tcont_entity_id));
-
-	INIT_WORK(&priv->irq_work, gpon_irq_work_fn);
-	INIT_DELAYED_WORK(&priv->to1_work, gpon_to1_work_fn);
-	INIT_DELAYED_WORK(&priv->to2_work, gpon_to2_work_fn);
-	INIT_DELAYED_WORK(&priv->restart_work, gpon_restart_work_fn);
-	atomic_set(&priv->pending_irqs, 0);
-
-	priv->irq = platform_get_irq(pdev, 0);
-	if (priv->irq < 0) {
-		ret = dev_err_probe(dev, priv->irq, "needs mac irq");
-		goto err_destroy_fsm_wq;
-	}
-
-	ret = devm_request_irq(dev, priv->irq, gpon_isr, 0,
-			       dev_name(dev), priv);
-	if (ret)
-		goto err_destroy_fsm_wq;
-	disable_irq(priv->irq);
-	priv->irq_enabled = false;
-	dev_info(dev, "GPON IRQ %d requested and initially disabled\n",
-		 priv->irq);
-
-	ret = gpon_load_credentials(priv);
-	if (ret)
-		goto err_destroy_fsm_wq;
-
-	priv->ber_interval_ms = 1000;
-
-	timer_setup(&priv->ber_timer, gpon_ber_timer_fn, 0);
-
-	priv->ploam = ploam_alloc(&gpon_ploam_ops, priv, priv->sn, priv->passwd);
-	if (!priv->ploam) {
-		ret = -ENOMEM;
-		goto err_destroy_fsm_wq;
-	}
-
-	priv->lddla_dev = airoha_xpon_find_lddla(dev);
-	if (IS_ERR(priv->lddla_dev)) {
-		ret = dev_err_probe(dev, PTR_ERR(priv->lddla_dev),
-				    "failed to find LDDLA device\n");
-		priv->lddla_dev = NULL;
-		goto err_free_ploam;
-	}
-
-	priv->sfp_bus = sfp_bus_find_fwnode(dev->fwnode);
-	if (!priv->sfp_bus) {
-		ret = -ENODEV;
-		dev_err(dev, "missing SFP reference\n");
-		goto err_put_lddla;
-	}
-	if (IS_ERR(priv->sfp_bus)) {
-		ret = PTR_ERR(priv->sfp_bus);
-		dev_err(dev, "failed to find SFP bus: %d\n", ret);
-		goto err_put_lddla;
-	}
-
-	ret = sfp_bus_add_upstream(priv->sfp_bus, priv, &gpon_sfp_ops);
-	if (ret)
-		goto err_put_sfp;
-
-	ret = airoha_gpon_omci_register(&priv->omci, dev, priv->gdm_dev,
-					priv, &priv->identity);
-	if (ret)
-		goto err_put_gdm;
-
-	/*
-	 * The OMCI device is zero-initialized. Publish O1 before the first
-	 * PLOAM state callback so userspace never observes an invalid O0.
-	 */
-	airoha_gpon_omci_set_state(&priv->omci, GPON_O1_INITIAL);
-
-	priv->omci_handler.rx = airoha_gpon_omci_receive;
-	priv->omci_handler.priv = &priv->omci;
-	ret = airoha_eth_register_xpon_oam(priv->gdm_dev,
-					   &priv->omci_handler);
-	if (ret)
-		goto err_unregister_omci;
-
-	ret = airoha_eth_register_xpon(priv->gdm_dev, AIROHA_XPON_MODE_GPON,
-				       &gpon_link_ops, priv);
-	if (ret)
-		goto err_unregister_oam;
-	airoha_xpon_update_netdev_link(priv->gdm_dev,
-				       AIROHA_XPON_MODE_GPON, false);
-
-	platform_set_drvdata(pdev, priv);
-	dev_info(dev,
-		 "GPON probe complete: datapath=%s omci-genl=%u irq=%d default_state=%s\n",
-		 priv->gdm_dev->name, omci_device_id(priv->omci.odev),
-		 priv->irq, gpon_state_name(ploam_get_state(priv->ploam)));
-	return 0;
-
-err_unregister_oam:
-	airoha_eth_unregister_xpon_oam(priv->gdm_dev,
-				       &priv->omci_handler);
-err_unregister_omci:
-	airoha_gpon_omci_unregister(&priv->omci);
-err_put_gdm:
-	dev_put(priv->gdm_dev);
-	priv->gdm_dev = NULL;
-	sfp_bus_del_upstream(priv->sfp_bus);
-err_put_sfp:
-	sfp_bus_put(priv->sfp_bus);
-err_put_lddla:
-	if (priv->lddla_dev)
-		put_device(priv->lddla_dev);
-err_free_ploam:
-	ploam_free(priv->ploam);
-err_destroy_fsm_wq:
-	destroy_workqueue(priv->fsm_wq);
-err_put_eth_node:
-	if (priv->gdm_dev) {
-		dev_put(priv->gdm_dev);
-		priv->gdm_dev = NULL;
-	}
-	of_node_put(priv->eth_node);
-	return ret;
-}
-
-static void gpon_remove(struct platform_device *pdev)
-{
-	struct gpon_priv *priv = platform_get_drvdata(pdev);
-
-	dev_info(priv->dev, "removing GPON MAC driver\n");
-	cancel_delayed_work_sync(&priv->restart_work);
-	airoha_eth_unregister_xpon(priv->gdm_dev, &gpon_link_ops, priv);
-	if (priv->irq_enabled)
-		gpon_disable(priv);
-	else {
-		cancel_work_sync(&priv->irq_work);
-		cancel_delayed_work_sync(&priv->to1_work);
-		cancel_delayed_work_sync(&priv->to2_work);
-	}
-	airoha_eth_unregister_xpon_oam(priv->gdm_dev,
-				       &priv->omci_handler);
-	airoha_gpon_omci_unregister(&priv->omci);
-	dev_put(priv->gdm_dev);
-	priv->gdm_dev = NULL;
-	sfp_bus_del_upstream(priv->sfp_bus);
-	sfp_bus_put(priv->sfp_bus);
-	if (priv->lddla_dev)
-		put_device(priv->lddla_dev);
-	ploam_free(priv->ploam);
-	destroy_workqueue(priv->fsm_wq);
-	of_node_put(priv->eth_node);
-}
-
 /* -------------------------------------------------------------------------
  * EPON implementation
  * ------------------------------------------------------------------------- */
@@ -2921,15 +2679,6 @@ static void gpon_remove(struct platform_device *pdev)
 #define EPON_EXT_SW_RST_BIT	BIT(31)
 #define EPON_RESET_DELAY_US	100
 
-#define EPON_MAX_LLID		8
-
-/* LLID registration states */
-enum llid_state {
-	LLID_STATE_WAIT		= 0,
-	LLID_STATE_REGISTERING	= 1,
-	LLID_STATE_REGISTERED	= 2,
-};
-
 static const char *epon_llid_state_name(enum llid_state state)
 {
 	switch (state) {
@@ -2944,51 +2693,26 @@ static const char *epon_llid_state_name(enum llid_state state)
 	}
 }
 
-struct epon_llid {
-	enum llid_state	state;
-	bool		valid;
-	u16		value;
-};
-
-struct epon_priv {
-	void __iomem		*base;
-	void __iomem		*rst_base;	/* optional: external SW reset reg */
-	struct device		*dev;
-	struct regmap		*scu;
-	struct phy		*phy;
-	bool			phy_initialized;
-	bool			phy_powered;
-	struct device_node	*eth_node;
-	struct net_device	*gdm_dev;
-	bool			started;
-	struct sfp_bus		*sfp_bus;
-	struct device		*lddla_dev;
-	int			irq;
-	bool			irq_enabled;
-
-	struct epon_llid	llid[EPON_MAX_LLID];
-	int			registered_llids;
 
-};
 
-static inline u32 epon_read(struct epon_priv *priv, u32 reg)
+static inline u32 epon_read(struct xpon_priv *priv, u32 reg)
 {
-	return readl(priv->base + reg);
+	return readl(priv->epon_reg + reg);
 }
 
-static inline void epon_write(struct epon_priv *priv, u32 reg, u32 val)
+static inline void epon_write(struct xpon_priv *priv, u32 reg, u32 val)
 {
-	writel(val, priv->base + reg);
+	writel(val, priv->epon_reg + reg);
 }
 
 /* --- LLID discovery status helpers --- */
 
-static u32 epon_llid_sts(struct epon_priv *priv, int idx)
+static u32 epon_llid_sts(struct xpon_priv *priv, int idx)
 {
 	return epon_read(priv, EPON_LLID0_DSCVRY_STS + idx * 4);
 }
 
-static void epon_llid_sts_write(struct epon_priv *priv, int idx, u32 val)
+static void epon_llid_sts_write(struct xpon_priv *priv, int idx, u32 val)
 {
 	epon_write(priv, EPON_LLID0_DSCVRY_STS + idx * 4, val);
 }
@@ -2999,7 +2723,7 @@ static void epon_llid_sts_write(struct epon_priv *priv, int idx, u32 val)
  * clear bits[31:30] (set unregistered) 10 times, then set bits[31:30]=01
  * while setting all lower bits (0x7FFFFFFF gives bit30=1, rest all-ones).
  */
-static void epon_llid_set_registering(struct epon_priv *priv, int idx)
+static void epon_llid_set_registering(struct xpon_priv *priv, int idx)
 {
 	u32 tmp;
 	int i;
@@ -3017,7 +2741,7 @@ static void epon_llid_set_registering(struct epon_priv *priv, int idx)
 
 /* --- MAC address programming (indirect via e_mac_addr_cfg) --- */
 
-static int epon_wait_mac_cfg(struct epon_priv *priv)
+static int epon_wait_mac_cfg(struct xpon_priv *priv)
 {
 	int i;
 
@@ -3034,7 +2758,7 @@ static int epon_wait_mac_cfg(struct epon_priv *priv)
  * Two indirect writes: dw_idx=0 for bytes[2-5] (low 32), dw_idx=1 for
  * bytes[0-1] (high 16).  Matches ref eponMacSetMacAddr().
  */
-static int epon_program_mac_address(struct epon_priv *priv, int llid_idx,
+static int epon_program_mac_address(struct xpon_priv *priv, int llid_idx,
 				    const u8 mac[ETH_ALEN])
 {
 	u32 mac_low  = ((u32)mac[2] << 24) | ((u32)mac[3] << 16) |
@@ -3071,7 +2795,7 @@ static int epon_program_mac_address(struct epon_priv *priv, int llid_idx,
  * Submit an MPCP command to hardware and poll for completion.
  * Setting CMD_DONE in the write triggers execution; poll until HW confirms.
  */
-static void epon_discv_cmd(struct epon_priv *priv, u32 cmd, int llid_idx)
+static void epon_discv_cmd(struct xpon_priv *priv, u32 cmd, int llid_idx)
 {
 	u32 ctrl = cmd | DSCVRY_CMD_DONE | (llid_idx & DSCVRY_TX_MPCP_LLID_MASK);
 
@@ -3084,7 +2808,7 @@ static void epon_discv_cmd(struct epon_priv *priv, u32 cmd, int llid_idx)
 
 /* --- Security key --- */
 
-static void epon_set_security_key(struct epon_priv *priv, int llid_idx,
+static void epon_set_security_key(struct xpon_priv *priv, int llid_idx,
 				  int key_idx, u8 key[16])
 {
 	int dw;
@@ -3106,18 +2830,18 @@ static void epon_set_security_key(struct epon_priv *priv, int llid_idx,
 
 /* --- SW reset sequence (ref: eponMacSwReset) --- */
 
-static void epon_sw_reset(struct epon_priv *priv)
+static void epon_sw_reset(struct xpon_priv *priv)
 {
 	u32 raw;
 
 	dev_info(priv->dev, "resetting EPON MAC (external_reset=%u)\n",
-		 !!priv->rst_base);
+		 !!priv->epon_reset_reg);
 	/* Assert external system-level SW reset if mapped. */
-	if (priv->rst_base) {
-		raw = readl(priv->rst_base);
-		writel(raw | EPON_EXT_SW_RST_BIT, priv->rst_base);
+	if (priv->epon_reset_reg) {
+		raw = readl(priv->epon_reset_reg);
+		writel(raw | EPON_EXT_SW_RST_BIT, priv->epon_reset_reg);
 		udelay(EPON_RESET_DELAY_US);
-		writel(raw & ~EPON_EXT_SW_RST_BIT, priv->rst_base);
+		writel(raw & ~EPON_EXT_SW_RST_BIT, priv->epon_reset_reg);
 	}
 
 	/* Assert EPON MAC internal SW reset (GLB_CFG bit 4). */
@@ -3149,7 +2873,7 @@ static void epon_sw_reset(struct epon_priv *priv)
  * select EPON WAN mode, clear GDM2 GPON release mode, stop the EPON MBI,
  * initialise MAC/LLID state, enable GDM2/CDM2 channels, then release MBI.
  */
-static int epon_prepare_hardware(struct epon_priv *priv)
+static int epon_prepare_hardware(struct xpon_priv *priv)
 {
 	int ret;
 
@@ -3169,7 +2893,7 @@ static int epon_prepare_hardware(struct epon_priv *priv)
 	return 0;
 }
 
-static void epon_hw_init(struct epon_priv *priv)
+static void epon_hw_init(struct xpon_priv *priv)
 {
 	u32 cfg;
 	int i;
@@ -3207,7 +2931,7 @@ static void epon_hw_init(struct epon_priv *priv)
 
 static irqreturn_t epon_isr(int irq, void *data)
 {
-	struct epon_priv *priv = data;
+	struct xpon_priv *priv = data;
 	u32 status;
 	int idx;
 
@@ -3371,7 +3095,7 @@ static irqreturn_t epon_isr(int irq, void *data)
 
 /* --- Enable / Disable --- */
 
-static int epon_enable(struct epon_priv *priv)
+static int epon_enable(struct xpon_priv *priv)
 {
 	int idx, ret;
 	u32 int_en;
@@ -3445,7 +3169,7 @@ static int epon_enable(struct epon_priv *priv)
 	return 0;
 }
 
-static void epon_disable(struct epon_priv *priv)
+static void epon_disable(struct xpon_priv *priv)
 {
 	int idx, ret;
 
@@ -3486,14 +3210,14 @@ static void epon_disable(struct epon_priv *priv)
 
 static void epon_sfp_attach(void *upstream, struct sfp_bus *bus)
 {
-	struct epon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "EPON SFP bus attached\n");
 }
 
 static void epon_sfp_detach(void *upstream, struct sfp_bus *bus)
 {
-	struct epon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "EPON SFP bus detached\n");
 }
@@ -3501,7 +3225,7 @@ static void epon_sfp_detach(void *upstream, struct sfp_bus *bus)
 static int epon_sfp_module_insert(void *upstream,
 				  const struct sfp_eeprom_id *id)
 {
-	struct epon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "EPON SFP module inserted\n");
 	return 0;
@@ -3509,7 +3233,7 @@ static int epon_sfp_module_insert(void *upstream,
 
 static void epon_sfp_module_remove(void *upstream)
 {
-	struct epon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "EPON SFP module removed\n");
 	epon_disable(priv);
@@ -3517,7 +3241,7 @@ static void epon_sfp_module_remove(void *upstream)
 
 static int epon_sfp_module_start(void *upstream)
 {
-	struct epon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 	int ret;
 
 	dev_info(priv->dev, "EPON SFP module start\n");
@@ -3531,7 +3255,7 @@ static int epon_sfp_module_start(void *upstream)
 
 static void epon_sfp_module_stop(void *upstream)
 {
-	struct epon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "EPON SFP module stop\n");
 	epon_disable(priv);
@@ -3539,7 +3263,7 @@ static void epon_sfp_module_stop(void *upstream)
 
 static void epon_sfp_link_down(void *upstream)
 {
-	struct epon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_warn(priv->dev, "EPON optical link down / LOS\n");
 	airoha_xpon_update_netdev_link(priv->gdm_dev,
@@ -3548,7 +3272,7 @@ static void epon_sfp_link_down(void *upstream)
 
 static void epon_sfp_link_up(void *upstream)
 {
-	struct epon_priv *priv = upstream;
+	struct xpon_priv *priv = upstream;
 
 	dev_info(priv->dev, "EPON optical link up\n");
 }
@@ -3568,7 +3292,7 @@ static const struct sfp_upstream_ops epon_sfp_ops = {
 
 static int epon_link_start(void *data)
 {
-	struct epon_priv *priv = data;
+	struct xpon_priv *priv = data;
 
 	if (READ_ONCE(priv->started))
 		return 0;
@@ -3583,7 +3307,7 @@ static int epon_link_start(void *data)
 
 static void epon_link_stop(void *data)
 {
-	struct epon_priv *priv = data;
+	struct xpon_priv *priv = data;
 
 	if (!READ_ONCE(priv->started))
 		return;
@@ -3599,32 +3323,189 @@ static const struct airoha_xpon_link_ops epon_link_ops = {
 	.stop = epon_link_stop,
 };
 
-/* ---------- Platform driver ---------- */
+/* -------------------------------------------------------------------------
+ * Unified platform driver
+ * ------------------------------------------------------------------------- */
+
+static const struct airoha_xpon_match_data xpon_data = {
+	.mode = AIROHA_XPON_MODE_GPON,
+};
+
+static bool airoha_xpon_is_gpon(struct xpon_priv *priv)
+{
+	return priv->mode == AIROHA_XPON_MODE_GPON;
+}
+
+static const struct sfp_upstream_ops *
+airoha_xpon_get_sfp_ops(struct xpon_priv *priv)
+{
+	if (airoha_xpon_is_gpon(priv))
+		return &gpon_sfp_ops;
+
+	return &epon_sfp_ops;
+}
+
+static const struct airoha_xpon_link_ops *
+airoha_xpon_get_link_ops(struct xpon_priv *priv)
+{
+	if (airoha_xpon_is_gpon(priv))
+		return &gpon_link_ops;
+
+	return &epon_link_ops;
+}
+
+static int airoha_xpon_request_mac_irq(struct platform_device *pdev,
+				       struct xpon_priv *priv,
+				       irq_handler_t handler)
+{
+	struct device *dev = &pdev->dev;
+	int ret;
+
+	priv->irq = platform_get_irq_byname(pdev, "mac");
+	if (priv->irq < 0)
+		priv->irq = platform_get_irq(pdev, 0);
+	if (priv->irq < 0)
+		return dev_err_probe(dev, priv->irq, "needs mac irq\n");
+
+	ret = devm_request_irq(dev, priv->irq, handler, 0, dev_name(dev), priv);
+	if (ret)
+		return ret;
+
+	disable_irq(priv->irq);
+	priv->irq_enabled = false;
+	dev_info(dev, "%s IRQ %d requested and initially disabled\n",
+		 airoha_xpon_mode_name(priv->mode), priv->irq);
+
+	return 0;
+}
+
+static int airoha_xpon_init_gpon(struct platform_device *pdev,
+				 struct xpon_priv *priv)
+{
+	struct device *dev = &pdev->dev;
+	int ret;
+
+	if (!priv->gpon_reg)
+		return dev_err_probe(dev, -EINVAL,
+				     "missing GPON register window\n");
+
+	priv->fsm_wq = alloc_ordered_workqueue("%s-gpon-fsm",
+					       WQ_MEM_RECLAIM, dev_name(dev));
+	if (!priv->fsm_wq)
+		return -ENOMEM;
+
+	mutex_init(&priv->tcont_lock);
+	memset(priv->tcont_alloc_id, 0xff, sizeof(priv->tcont_alloc_id));
+	memset(priv->tcont_entity_id, 0xff, sizeof(priv->tcont_entity_id));
+
+	INIT_WORK(&priv->irq_work, gpon_irq_work_fn);
+	INIT_DELAYED_WORK(&priv->to1_work, gpon_to1_work_fn);
+	INIT_DELAYED_WORK(&priv->to2_work, gpon_to2_work_fn);
+	INIT_DELAYED_WORK(&priv->restart_work, gpon_restart_work_fn);
+	atomic_set(&priv->pending_irqs, 0);
+
+	ret = airoha_xpon_request_mac_irq(pdev, priv, gpon_isr);
+	if (ret)
+		goto err_destroy_fsm_wq;
+
+	ret = gpon_load_credentials(priv);
+	if (ret)
+		goto err_destroy_fsm_wq;
+
+	priv->ber_interval_ms = 1000;
+	timer_setup(&priv->ber_timer, gpon_ber_timer_fn, 0);
+
+	priv->ploam = ploam_alloc(&gpon_ploam_ops, priv, priv->hw_sn,
+				  priv->hw_passwd);
+	if (!priv->ploam) {
+		ret = -ENOMEM;
+		goto err_destroy_fsm_wq;
+	}
+
+	return 0;
+
+err_destroy_fsm_wq:
+	destroy_workqueue(priv->fsm_wq);
+	priv->fsm_wq = NULL;
+	return ret;
+}
+
+static void airoha_xpon_cleanup_gpon(struct xpon_priv *priv)
+{
+	ploam_free(priv->ploam);
+	priv->ploam = NULL;
+
+	if (priv->fsm_wq) {
+		destroy_workqueue(priv->fsm_wq);
+		priv->fsm_wq = NULL;
+	}
+}
+
+static int airoha_xpon_init_epon(struct platform_device *pdev,
+				 struct xpon_priv *priv)
+{
+	if (!priv->epon_reg)
+		return dev_err_probe(&pdev->dev, -EINVAL,
+				     "missing EPON register window\n");
+
+	return airoha_xpon_request_mac_irq(pdev, priv, epon_isr);
+}
+
+static int airoha_xpon_register_gpon_omci(struct xpon_priv *priv)
+{
+	int ret;
+
+	ret = airoha_gpon_omci_register(&priv->omci, priv->dev, priv->gdm_dev,
+					priv, &priv->identity);
+	if (ret)
+		return ret;
+
+	/*
+	 * The OMCI device is zero-initialized. Publish O1 before the first
+	 * PLOAM state callback so userspace never observes an invalid O0.
+	 */
+	airoha_gpon_omci_set_state(&priv->omci, GPON_O1_INITIAL);
+
+	priv->omci_handler.rx = airoha_gpon_omci_receive;
+	priv->omci_handler.priv = &priv->omci;
+	ret = airoha_eth_register_xpon_oam(priv->gdm_dev,
+					   &priv->omci_handler);
+	if (ret) {
+		airoha_gpon_omci_unregister(&priv->omci);
+		return ret;
+	}
+
+	return 0;
+}
+
+static void airoha_xpon_unregister_gpon_omci(struct xpon_priv *priv)
+{
+	airoha_eth_unregister_xpon_oam(priv->gdm_dev, &priv->omci_handler);
+	airoha_gpon_omci_unregister(&priv->omci);
+}
 
-static int epon_probe(struct platform_device *pdev)
+static int airoha_xpon_probe(struct platform_device *pdev)
 {
 	struct device *dev = &pdev->dev;
-	struct epon_priv *priv;
+	const struct airoha_xpon_match_data *data;
+	const struct airoha_xpon_link_ops *link_ops;
+	const struct sfp_upstream_ops *sfp_ops;
+	struct xpon_priv *priv;
 	struct resource *res;
 	int ret;
 
-	dev_info(dev, "probing EPON MAC\n");
+	data = device_get_match_data(&pdev->dev);
+	if (!data)
+		return -EINVAL;
+
 	priv = devm_kzalloc(dev, sizeof(*priv), GFP_KERNEL);
 	if (!priv)
 		return -ENOMEM;
 	priv->dev = dev;
+	priv->mode = data->mode;
 
-	priv->eth_node = of_parse_phandle(dev->of_node, "ethernet", 0);
-	if (!priv->eth_node) {
-		ret = dev_err_probe(dev, -ENODEV,
-				    "missing ethernet phandle for EPON datapath\n");
-		goto err_put_eth_node;
-	}
-	dev_info(dev, "EPON datapath phandle: %pOF\n", priv->eth_node);
-
-	ret = airoha_xpon_find_gdm2(dev, priv->eth_node, &priv->gdm_dev);
-	if (ret)
-		goto err_put_eth_node;
+	dev_info(dev, "xPON initial mode: %s\n",
+		 airoha_xpon_mode_name(priv->mode));
 
 	priv->scu = syscon_regmap_lookup_by_phandle(dev->of_node,
 					    "airoha,scu");
@@ -3641,79 +3522,88 @@ static int epon_probe(struct platform_device *pdev)
 		goto err_put_eth_node;
 	}
 
+	priv->eth_node = of_parse_phandle(dev->of_node, "ethernet", 0);
+	if (!priv->eth_node) {
+		ret = dev_err_probe(dev, -EINVAL,
+				    "missing Ethernet datapath phandle\n");
+		goto err_put_eth_node;
+	}
+	dev_info(dev, "%s datapath phandle: %pOF\n",
+		 airoha_xpon_mode_name(priv->mode), priv->eth_node);
+
+	ret = airoha_xpon_find_gdm2(dev, priv->eth_node, &priv->gdm_dev);
+	if (ret)
+		goto err_put_eth_node;
+
 	res = platform_get_resource_byname(pdev, IORESOURCE_MEM, "mac");
 	if (!res)
 		res = platform_get_resource(pdev, IORESOURCE_MEM, 0);
 	if (!res) {
-		ret = dev_err_probe(dev, -EINVAL, "missing EPON MAC resource\n");
+		ret = dev_err_probe(dev, -EINVAL, "missing mac resource\n");
 		goto err_put_eth_node;
 	}
 
 	priv->base = devm_ioremap_resource(dev, res);
 	if (IS_ERR(priv->base)) {
-		ret = PTR_ERR(priv->base);
+		ret = dev_err_probe(dev, PTR_ERR(priv->base),
+				    "needs xPON mac base\n");
 		goto err_put_eth_node;
 	}
 
-	if (res->start == EN7523_XPON_REGION_BASE) {
-		if (resource_size(res) < EN7523_EPON_REG_OFFSET + 0x400) {
-			ret = dev_err_probe(dev, -EINVAL,
-					    "xPON region too small for EPON: %pR\n",
-					    res);
-			goto err_put_eth_node;
-		}
-		priv->base += EN7523_EPON_REG_OFFSET;
-	} else if (res->start != EN7523_EPON_REG_BASE) {
-		ret = dev_err_probe(dev, -EINVAL,
-				    "unsupported EPON MAC resource: %pR\n", res);
-		goto err_put_eth_node;
-	} else if (resource_size(res) < 0x400) {
+	/*
+	 * All supported xPON MAC blocks expose GPON and EPON at fixed offsets.
+	 * Add a larger resource-size case here when XGSPON support lands.
+	 */
+	if (resource_size(res) < V1_XPON_REGION_SIZE) {
 		ret = dev_err_probe(dev, -EINVAL,
-				    "EPON register window is too small: %pR\n", res);
+				    "unsupported xPON MAC resource size %#llx: %pR\n",
+				    (unsigned long long)resource_size(res),
+				    res);
 		goto err_put_eth_node;
 	}
+	
+	switch (data->mode) {
+	case AIROHA_XPON_MODE_XGSPON:
+		priv->xgspon_reg = priv->base + XGSPON_REG_OFFSET;
+		fallthrough;
+	case AIROHA_XPON_MODE_GPON:
+	case AIROHA_XPON_MODE_EPON:
+	default:
+		priv->gpon_reg = priv->base + GPON_REG_OFFSET;
+		priv->epon_reg = priv->base + EPON_REG_OFFSET;
+		break;
+	}
 
-	dev_info(dev, "EPON resource %pR, register base offset %#x\n",
-		 res, res->start == EN7523_XPON_REGION_BASE ?
-		 EN7523_EPON_REG_OFFSET : 0);
-
-	/* Optional second resource: external SW reset register */
 	res = platform_get_resource_byname(pdev, IORESOURCE_MEM,
 					   "epon-reset");
 	if (res) {
-		priv->rst_base = devm_ioremap_resource(dev, res);
-		if (IS_ERR(priv->rst_base))
-			priv->rst_base = NULL;
-	}
-
-	priv->irq = platform_get_irq(pdev, 0);
-	if (priv->irq < 0) {
-		ret = priv->irq;
-		goto err_put_eth_node;
+		priv->epon_reset_reg = devm_ioremap_resource(dev, res);
+		if (IS_ERR(priv->epon_reset_reg)) {
+			ret = dev_err_probe(dev, PTR_ERR(priv->epon_reset_reg),
+					    "failed to map EPON reset register\n");
+			goto err_put_eth_node;
+		}
 	}
 
-	ret = devm_request_irq(dev, priv->irq, epon_isr, 0,
-			       dev_name(dev), priv);
+	if (airoha_xpon_is_gpon(priv))
+		ret = airoha_xpon_init_gpon(pdev, priv);
+	else
+		ret = airoha_xpon_init_epon(pdev, priv);
 	if (ret)
 		goto err_put_eth_node;
 
-	/* IRQ starts disabled; enabled in epon_enable() when SFP is ready */
-	disable_irq(priv->irq);
-	priv->irq_enabled = false;
-	dev_info(dev, "EPON IRQ %d requested and initially disabled\n",
-		 priv->irq);
-
 	priv->lddla_dev = airoha_xpon_find_lddla(dev);
 	if (IS_ERR(priv->lddla_dev)) {
 		ret = dev_err_probe(dev, PTR_ERR(priv->lddla_dev),
 				    "failed to find LDDLA device\n");
 		priv->lddla_dev = NULL;
-		goto err_put_eth_node;
+		goto err_cleanup_mode;
 	}
 
 	priv->sfp_bus = sfp_bus_find_fwnode(dev->fwnode);
 	if (!priv->sfp_bus) {
-		ret = dev_err_probe(dev, -ENODEV, "missing SFP reference\n");
+		ret = -ENODEV;
+		dev_err(dev, "missing SFP reference\n");
 		goto err_put_lddla;
 	}
 	if (IS_ERR(priv->sfp_bus)) {
@@ -3722,22 +3612,40 @@ static int epon_probe(struct platform_device *pdev)
 		goto err_put_lddla;
 	}
 
-	ret = sfp_bus_add_upstream(priv->sfp_bus, priv, &epon_sfp_ops);
+	sfp_ops = airoha_xpon_get_sfp_ops(priv);
+	ret = sfp_bus_add_upstream(priv->sfp_bus, priv, sfp_ops);
 	if (ret)
 		goto err_put_sfp;
 
-	ret = airoha_eth_register_xpon(priv->gdm_dev, AIROHA_XPON_MODE_EPON,
-				       &epon_link_ops, priv);
+	if (airoha_xpon_is_gpon(priv)) {
+		ret = airoha_xpon_register_gpon_omci(priv);
+		if (ret)
+			goto err_del_upstream;
+	}
+
+	link_ops = airoha_xpon_get_link_ops(priv);
+	ret = airoha_eth_register_xpon(priv->gdm_dev, priv->mode, link_ops,
+				       priv);
 	if (ret)
-		goto err_del_upstream;
+		goto err_unregister_gpon_omci;
+
 	airoha_xpon_update_netdev_link(priv->gdm_dev,
-				       AIROHA_XPON_MODE_EPON, false);
+				       priv->mode, false);
 
 	platform_set_drvdata(pdev, priv);
-	dev_info(dev, "EPON probe complete: datapath=%s irq=%d\n",
-		 priv->gdm_dev->name, priv->irq);
+	if (airoha_xpon_is_gpon(priv))
+		dev_info(dev,
+			 "GPON probe complete: datapath=%s omci-genl=%u irq=%d default_state=%s\n",
+			 priv->gdm_dev->name, omci_device_id(priv->omci.odev),
+			 priv->irq, gpon_state_name(ploam_get_state(priv->ploam)));
+	else
+		dev_info(dev, "EPON probe complete: datapath=%s irq=%d\n",
+			 priv->gdm_dev->name, priv->irq);
 	return 0;
 
+err_unregister_gpon_omci:
+	if (airoha_xpon_is_gpon(priv))
+		airoha_xpon_unregister_gpon_omci(priv);
 err_del_upstream:
 	sfp_bus_del_upstream(priv->sfp_bus);
 err_put_sfp:
@@ -3745,6 +3653,9 @@ static int epon_probe(struct platform_device *pdev)
 err_put_lddla:
 	if (priv->lddla_dev)
 		put_device(priv->lddla_dev);
+err_cleanup_mode:
+	if (airoha_xpon_is_gpon(priv))
+		airoha_xpon_cleanup_gpon(priv);
 err_put_eth_node:
 	if (priv->gdm_dev) {
 		dev_put(priv->gdm_dev);
@@ -3754,90 +3665,61 @@ static int epon_probe(struct platform_device *pdev)
 	return ret;
 }
 
-static void epon_remove(struct platform_device *pdev)
+static void airoha_xpon_remove(struct platform_device *pdev)
 {
-	struct epon_priv *priv = platform_get_drvdata(pdev);
+	const struct airoha_xpon_link_ops *link_ops;
+	struct xpon_priv *priv;
+
+	priv = platform_get_drvdata(pdev);
+	if (!priv)
+		return;
+
+	dev_info(priv->dev, "removing xPON mode %s\n",
+		 airoha_xpon_mode_name(priv->mode));
+
+	if (airoha_xpon_is_gpon(priv))
+		cancel_delayed_work_sync(&priv->restart_work);
+
+	link_ops = airoha_xpon_get_link_ops(priv);
+	airoha_eth_unregister_xpon(priv->gdm_dev, link_ops, priv);
+
+	if (priv->irq_enabled) {
+		switch (priv->mode) {
+		case AIROHA_XPON_MODE_EPON:
+			epon_disable(priv);
+			break;
+		case AIROHA_XPON_MODE_GPON:
+		case AIROHA_XPON_MODE_XGSPON:
+			gpon_disable(priv);
+			break;
+		}
+	} else {
+		if (airoha_xpon_is_gpon(priv)) {
+			cancel_work_sync(&priv->irq_work);
+			cancel_delayed_work_sync(&priv->to1_work);
+			cancel_delayed_work_sync(&priv->to2_work);
+		}
+	}
+
+	if (airoha_xpon_is_gpon(priv))
+		airoha_xpon_unregister_gpon_omci(priv);
 
-	dev_info(priv->dev, "removing EPON MAC driver\n");
-	airoha_eth_unregister_xpon(priv->gdm_dev, &epon_link_ops, priv);
-	if (priv->irq_enabled)
-		epon_disable(priv);
 	sfp_bus_del_upstream(priv->sfp_bus);
 	sfp_bus_put(priv->sfp_bus);
 	if (priv->lddla_dev)
 		put_device(priv->lddla_dev);
+	if (airoha_xpon_is_gpon(priv))
+		airoha_xpon_cleanup_gpon(priv);
 	dev_put(priv->gdm_dev);
 	priv->gdm_dev = NULL;
 	of_node_put(priv->eth_node);
 }
 
-/* -------------------------------------------------------------------------
- * Unified platform driver
- * ------------------------------------------------------------------------- */
-
-static const struct airoha_xpon_match_data en7523_xpon_data = {
-	.mode = AIROHA_XPON_MODE_GPON,
-	.mode_from_dt = true,
-};
-
-static const struct airoha_xpon_match_data gpon_data = {
-	.mode = AIROHA_XPON_MODE_GPON,
-};
-
-static const struct airoha_xpon_match_data epon_data = {
-	.mode = AIROHA_XPON_MODE_EPON,
-};
-
-static int airoha_xpon_probe(struct platform_device *pdev)
-{
-	const struct airoha_xpon_match_data *data;
-	enum airoha_xpon_mode mode;
-	int ret;
-
-	data = device_get_match_data(&pdev->dev);
-	if (!data)
-		return -EINVAL;
-
-	ret = airoha_xpon_get_mode(&pdev->dev, data, &mode);
-	if (ret)
-		return ret;
-
-	dev_info(&pdev->dev, "xPON mode selected: %s (%s)\n",
-		 airoha_xpon_mode_name(mode),
-		 data->mode_from_dt ? "Device Tree/default" : "compatible");
-	if (mode == AIROHA_XPON_MODE_GPON)
-		return gpon_probe(pdev);
-
-	return epon_probe(pdev);
-}
-
-static void airoha_xpon_remove(struct platform_device *pdev)
-{
-	const struct airoha_xpon_match_data *data;
-	enum airoha_xpon_mode mode;
-
-	data = device_get_match_data(&pdev->dev);
-	if (!data || airoha_xpon_get_mode(&pdev->dev, data, &mode))
-		return;
-
-	dev_info(&pdev->dev, "removing xPON mode %s\n",
-		 airoha_xpon_mode_name(mode));
-	if (mode == AIROHA_XPON_MODE_GPON)
-		gpon_remove(pdev);
-	else
-		epon_remove(pdev);
-}
-
 static const struct of_device_id airoha_xpon_of_match[] = {
-	{ .compatible = "airoha,en7523-xpon", .data = &en7523_xpon_data },
-	{ .compatible = "airoha,en7523-gpon", .data = &gpon_data },
-	{ .compatible = "airoha,en7523-epon", .data = &epon_data },
-	{ .compatible = "econet,en7521-gpon", .data = &gpon_data },
-	{ .compatible = "econet,en7526-gpon", .data = &gpon_data },
-	{ .compatible = "econet,en751221-gpon", .data = &gpon_data },
-	{ .compatible = "econet,en7521-epon", .data = &epon_data },
-	{ .compatible = "econet,en7526-epon", .data = &epon_data },
-	{ .compatible = "econet,en751221-epon", .data = &epon_data },
+	{ .compatible = "airoha,en7523-xpon", .data = &xpon_data },
+	{ .compatible = "econet,en7521-xpon", .data = &xpon_data },
+	{ .compatible = "econet,en7526-xpon", .data = &xpon_data },
+	{ .compatible = "econet,en751221-xpon", .data = &xpon_data },
 	{}
 };
 MODULE_DEVICE_TABLE(of, airoha_xpon_of_match);
@@ -3853,6 +3735,7 @@ static struct platform_driver airoha_xpon_driver = {
 
 module_platform_driver(airoha_xpon_driver);
 
-MODULE_DESCRIPTION("Airoha/EcoNet unified GPON and EPON MAC driver");
+MODULE_DESCRIPTION("Airoha PON MAC driver");
+MODULE_AUTHOR("Matheus Sampaio Queiroga <srherobrine20@gmail.com>");
 MODULE_AUTHOR("Benjamin Larsson <benjamin.larsson@genexis.eu>");
 MODULE_LICENSE("GPL");
-- 
2.55.0

