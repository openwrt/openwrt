@SERIAL_CONFIG@
@TERMINAL_CONFIG@

set default="0"
set timeout="@TIMEOUT@"

if [ "${grub_platform}" = "efi" ]; then
    set root='(cd0)'
else
    set root='(cd)'
fi

menuentry "@TITLE@" {
	if [ -s /boot/intel-ucode.img ]; then
		set ucode=/boot/intel-ucode.img
	fi
	if [ -s /boot/amd-ucode.img ]; then
		set ucode=$ucode /boot/amd-ucode.img
	fi
	if [ -z $ucode ]; then
		linux /boot/vmlinuz root=/dev/sr0 rootfstype=iso9660 rootwait @CMDLINE@ noinitrd
	else
		linux /boot/vmlinuz root=/dev/sr0 rootfstype=iso9660 rootwait @CMDLINE@
		initrd $ucode
	fi
}
