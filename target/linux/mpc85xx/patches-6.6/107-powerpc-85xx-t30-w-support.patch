--- a/arch/powerpc/platforms/85xx/Kconfig
+++ b/arch/powerpc/platforms/85xx/Kconfig
@@ -71,6 +71,17 @@ config PANDA
 	  This board is a Concurrent Dual-Band wireless access point with a
 	  Freescale P1020 SoC.
 
+config WG_T30W
+	bool "WatchGuard T30-W"
+	select DEFAULT_UIMAGE
+	select ARCH_REQUIRE_GPIOLIB
+	select GPIO_MPC8XXX
+	help
+	  This option enables support for the WatchGuard T30-W board.
+
+	  This board is a wireless access point with a
+	  Freescale P1011 SoC.
+
 config MPC8540_ADS
 	bool "Freescale MPC8540 ADS"
 	select DEFAULT_UIMAGE
--- a/arch/powerpc/platforms/85xx/Makefile
+++ b/arch/powerpc/platforms/85xx/Makefile
@@ -25,6 +25,7 @@
 obj-$(CONFIG_PANDA)       += panda.o
 obj-$(CONFIG_PPC_P2020)   += p2020.o $(obj8259-y)
 obj-$(CONFIG_TWR_P102x)   += twr_p102x.o
+obj-$(CONFIG_WG_T30W)     += wg-t30w.o
 obj-$(CONFIG_WS_AP3710I)  += ws-ap3710i.o
 obj-$(CONFIG_WS_AP3825I)  += ws-ap3825i.o
 obj-$(CONFIG_CORENET_GENERIC)   += corenet_generic.o
--- a/arch/powerpc/boot/Makefile
+++ b/arch/powerpc/boot/Makefile
@@ -182,6 +182,7 @@
 src-plat-$(CONFIG_PPC_POWERNV) += pseries-head.S
 src-plat-$(CONFIG_PPC_IBM_CELL_BLADE) += pseries-head.S
 src-plat-$(CONFIG_MVME7100) += motload-head.S mvme7100.c
+src-plat-$(CONFIG_WG_T30W) += simpleboot.c fixed-head.S
 src-plat-$(CONFIG_HIVEAP_330) += simpleboot.c fixed-head.S
 src-plat-$(CONFIG_TL_WDR4900_V1) += simpleboot.c fixed-head.S
 src-plat-$(CONFIG_WS_AP3710I) += simpleboot.c fixed-head.S
@@ -357,6 +358,7 @@
 image-$(CONFIG_TQM8555)			+= cuImage.tqm8555
 image-$(CONFIG_TQM8560)			+= cuImage.tqm8560
 image-$(CONFIG_KSI8560)			+= cuImage.ksi8560
+image-$(CONFIG_WG_T30W)			+= simpleImage.t30-w
 image-$(CONFIG_HIVEAP_330)		+= simpleImage.hiveap-330
 image-$(CONFIG_TL_WDR4900_V1)		+= simpleImage.tl-wdr4900-v1
 image-$(CONFIG_WS_AP3710I)		+= simpleImage.ws-ap3710i
--- a/arch/powerpc/boot/wrapper
+++ b/arch/powerpc/boot/wrapper
@@ -350,6 +350,7 @@
     platformo="$object/fixed-head.o $object/redboot-8xx.o"
     binary=y
     ;;
+simpleboot-t30-w|\
 simpleboot-hiveap-330|\
 simpleboot-tl-wdr4900-v1|\
 simpleboot-ws-ap3710i|\
--- a/arch/powerpc/platforms/85xx/Kconfig
+++ b/arch/powerpc/platforms/85xx/Kconfig
@@ -88,6 +88,7 @@
 	select DEFAULT_UIMAGE
 	select ARCH_REQUIRE_GPIOLIB
 	select GPIO_MPC8XXX
+	select PPC_ZIMAGE_LA3000000
 	help
 	  This option enables support for the WatchGuard T30-W board.

