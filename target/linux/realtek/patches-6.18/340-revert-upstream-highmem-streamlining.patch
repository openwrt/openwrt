--- a/mm/memblock.c
+++ b/mm/memblock.c
@@ -2226,10 +2226,7 @@ static unsigned long __init __free_memor
 				 phys_addr_t end)
 {
 	unsigned long start_pfn = PFN_UP(start);
-	unsigned long end_pfn = PFN_DOWN(end);
-
-	if (!IS_ENABLED(CONFIG_HIGHMEM) && end_pfn > max_low_pfn)
-		end_pfn = max_low_pfn;
+	unsigned long end_pfn = min_t(unsigned long, PFN_DOWN(end), max_low_pfn);
 
 	if (start_pfn >= end_pfn)
 		return 0;
--- a/mm/mm_init.c
+++ b/mm/mm_init.c
@@ -2665,6 +2665,25 @@ static void __init mem_init_print_info(v
 		);
 }
 
+static inline void __init mem_init_free_highmem(void)
+{
+#ifdef CONFIG_HIGHMEM
+	unsigned long tmp;
+
+	if (cpu_has_dc_aliases)
+		return;
+
+	for (tmp = highstart_pfn; tmp < highend_pfn; tmp++) {
+		struct page *page = pfn_to_page(tmp);
+
+		if (!memblock_is_memory(PFN_PHYS(tmp)))
+			SetPageReserved(page);
+		else
+			free_reserved_page(page);
+	}
+#endif
+}
+
 void __init __weak arch_mm_preinit(void)
 {
 }
@@ -2703,6 +2722,7 @@ void __init mm_core_init(void)
 	 */
 	kho_memory_init();
 
+	mem_init_free_highmem();
 	memblock_free_all();
 	mem_init();
 	kmem_cache_init();
