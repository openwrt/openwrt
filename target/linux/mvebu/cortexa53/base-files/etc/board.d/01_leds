#!/bin/sh

. /lib/functions/uci-defaults.sh

board=$(board_name)

board_config_update

case $board in
glinet,gl-mv1000)
	ucidef_set_led_netdev "wan" "WAN" "green:wan" "wan"
	;;
esac

board_config_flush

exit 0
