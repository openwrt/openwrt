#!/bin/sh

[ -e /lib/firmware/$FIRMWARE ] && exit 0

. /lib/functions/caldata.sh

board=$(board_name)

case "$FIRMWARE" in
"ath11k/IPQ9574/hw1.0/cal-ahb-c000000.wifi.bin")
	case "$board" in
	xiaomi,be7000)
		caldata_extract "0:art" 0x1000 0x20000
		;;
	esac
	;;
*)
	exit 1
	;;
esac
