--- a/misc/Makefile
+++ b/misc/Makefile
@@ -5,9 +5,9 @@

 include ../Config

-ifeq ($(HAVE_BERKELEY_DB),y)
-	TARGETS += arpd
-endif
+#ifeq ($(HAVE_BERKELEY_DB),y)
+#	TARGETS += arpd
+#endif

 ifeq ($(HAVE_SELINUX),y)
	LDLIBS += $(shell pkg-config --libs libselinux)

