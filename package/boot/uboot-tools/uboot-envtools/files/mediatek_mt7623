#
# Copyright (C) 2021 OpenWrt.org
#

if [ -e /etc/config/ubootenv ]; then
	[ "$(uci -q get ubootenv.@ubootenv[0].dev)" = "/dev/p1" ] || exit 0
	rm -f /etc/config/ubootenv /etc/fw_env.config
fi

touch /etc/config/ubootenv

. /lib/uboot-envtools.sh
. /lib/functions.sh

board=$(board_name)

case "$board" in
bananapi,bpi-r2)
	. /lib/upgrade/fit.sh
	export_fitblk_bootdev
	[ -n "$EMMC_KERN_DEV" ] || exit 1
	bootdev=${EMMC_KERN_DEV%%p[0-9]*}
	ubootenv_add_uci_config "${bootdev}p1" "0xb0000" "0x10000" "0x10000" "1"
	;;
unielec,u7623-02)
	ubootenv_add_uci_config "/dev/mmcblk0p1" "0xc0000" "0x10000" "0x10000" "1"
	;;
esac

config_load ubootenv
config_foreach ubootenv_add_app_config ubootenv

exit 0
