--- a/m4/lib_ifxos.m4
+++ b/m4/lib_ifxos.m4
@@ -90,7 +90,7 @@ dnl   fi
    if test -e $_libifxos_incl_with/ifxos_version.h; then
       AC_COMPILE_IFELSE(
             [AC_LANG_PROGRAM([[#include "ifxos_version.h"]],
-                             [[return;]])],
+                             [[return 0;]])],
             [AC_MSG_RESULT([$_libifxos_incl_with])],
             [AC_MSG_ERROR([Invalid ifxos_version.h file in '$_libifxos_incl_with'])]
          )
@@ -162,7 +162,7 @@ AC_DEFUN([LIBIFXOS_CHECK_CONFIG_INCL],
    if test -e $_libifxos_incl_path/ifxos_version.h; then
       AC_COMPILE_IFELSE(
             [AC_LANG_PROGRAM([[#include "ifxos_version.h"]],
-                             [[return;]])],
+                             [[return 0;]])],
             [AC_MSG_RESULT([$_libifxos_incl_path])],
             [AC_MSG_ERROR([Invalid ifxos_version.h file in '$_libifxos_incl_path'])]
          )
--- a/src/drv_tapi_event.h
+++ b/src/drv_tapi_event.h
@@ -102,6 +102,9 @@ extern IFX_int32_t IFX_TAPI_EventFifoCou
 extern IFX_int32_t IFX_TAPI_Event_Dispatch_ProcessCtx (
    IFX_TAPI_EXT_EVENT_PARAM_t *pParam);
 
+extern IFX_int32_t IFX_TAPI_EventWrpBufferPool_ElementAvailCountGet(void);
+extern IFX_int32_t IFX_TAPI_EventWrpBufferPool_ElementCountGet(void);
+
 extern IFX_int32_t TAPI_Event_Get (
    IFX_TAPI_DRV_CTX_t *pDrvCtx,
    IFX_TAPI_EVENT_t *pEvent);
--- a/src/drv_tapi_linux.c
+++ b/src/drv_tapi_linux.c
@@ -192,7 +192,7 @@ typedef int (*proc_callback_t)(struct se
  * field of proc_file_entry structure.  */
 typedef int (*proc_init_callback_t)(void);
 /* write function */
-typedef ssize_t (*proc_write_t)(struct file *file, const char __user *buffer,
+typedef ssize_t (*proc_write_tt)(struct file *file, const char __user *buffer,
    size_t count, loff_t *data);
 
 static void *TAPI_seq_start(struct seq_file *s, loff_t *pos);
@@ -214,8 +214,8 @@ struct proc_entry {
    proc_single_callback_t single_callback;
    proc_callback_t callback;
    proc_init_callback_t init_callback;
-   proc_write_t write_function;
-   struct file_operations ops;
+   proc_write_tt write_function;
+   struct proc_ops ops;
 };
 
 static struct seq_operations TAPI_seq_ops = {
@@ -353,9 +353,9 @@ static struct proc_entry proc_entries_pp
    {"status",  NULL, tapi_proc_ppd_read_status, proc_get_dev_count, NULL}
 };
 
-static const struct file_operations ppd_device_proc_fops = {
-   .open       = tapi_proc_ppd_wr_open,
-   .write      = tapi_proc_ppd_wr_device
+static const struct proc_ops ppd_device_proc_fops = {
+   .proc_open       = tapi_proc_ppd_wr_open,
+   .proc_write      = tapi_proc_ppd_wr_device
 };
 #endif /* TAPI_FEAT_PHONE_DETECTION_PROCFS */
 
@@ -470,11 +470,11 @@ IFX_return_t TAPI_OS_RegisterLLDrv (IFX_
 #endif /* not TAPI_ONE_DEVNODE */
 
 #ifdef ENABLE_HOTPLUG
-   tapi_class = class_create(THIS_MODULE, "tapidev");
+   tapi_class = class_create("tapidev");
    if (IS_ERR(tapi_class))
    {
       ret = PTR_ERR(tapi_class);
-      return TAPI_statusErr;
+      return IFX_ERROR;
    }
    for (j = 0; j < pLLDrvCtx->maxDevs; j++)
       for (i = 0; i <= pLLDrvCtx->maxChannels; i++)
@@ -482,7 +482,7 @@ IFX_return_t TAPI_OS_RegisterLLDrv (IFX_
          device = device_create(tapi_class, NULL, MKDEV(majorNumber, 10 + i),
                       NULL, "%s%d%d", pLLDrvCtx->devNodeName, j+1, i);
          if (IS_ERR(device))
-            return TAPI_statusErr;
+            return IFX_ERROR;
       }
 #endif /* #ifdef ENABLE_HOTPLUG*/
 
@@ -505,7 +505,7 @@ IFX_return_t TAPI_OS_RegisterLLDrv (IFX_
       TRACE( TAPI_DRV, DBG_LEVEL_HIGH,
            ("TAPI_OS_RegisterLLDrv: unable to register chrdev"
             " using major number %d\n\r", majorNumber));
-      return TAPI_statusErr;
+      return IFX_ERROR;
    }
 #else /* >= Linux 2.6.0 */
    /* Reserve the device number range. */
@@ -536,7 +536,7 @@ IFX_return_t TAPI_OS_RegisterLLDrv (IFX_
            ("TAPI_OS_RegisterLLDrv: unable to register chrdev number range"
             " using major number %d with %d minors\n\r",
             majorNumber, pHLDrvCtx->nDevNrCount));
-      return TAPI_statusErr;
+      return IFX_ERROR;
    }
 
    for (nDevIdx = 0; nDevIdx < maxDevices; nDevIdx++)
@@ -560,7 +560,7 @@ IFX_return_t TAPI_OS_RegisterLLDrv (IFX_
                ("TAPI: unable to add chrdev %s\n\r", pLLDrvCtx->devNodeName));
 
          cdev_del (&(pLLDrvCtx->pTapiDev[nDevIdx].cdev));
-         return TAPI_statusErr;
+         return IFX_ERROR;
       }
 
       for (nChIdx = 0; nChIdx <= maxChannels; nChIdx++)
@@ -609,7 +609,7 @@ IFX_return_t TAPI_OS_RegisterLLDrv (IFX_
             TRACE( TAPI_DRV, DBG_LEVEL_HIGH,
                  ("IFX_TAPI_Register_LL_Drv: unable to create class device "
                   "'%s'\n\r", sDevName));
-            return TAPI_statusErr;
+            return IFX_ERROR;
          }
 
          nTapiFdIdx++;
@@ -617,7 +617,7 @@ IFX_return_t TAPI_OS_RegisterLLDrv (IFX_
    }
 #endif /* (LINUX_VERSION_CODE >= KERNEL_VERSION(2,6,0)) */
 
-   return TAPI_statusOk;
+   return IFX_SUCCESS;
 }
 
 /**
@@ -677,7 +677,7 @@ IFX_return_t TAPI_OS_UnregisterLLDrv (IF
                              pHLDrvCtx->nDevNrCount);
 #endif
 
-   return TAPI_statusOk;
+   return IFX_SUCCESS;
 }
 
 /**
@@ -1824,7 +1824,7 @@ static int TAPI_proc_open(struct inode *
       return -ENOMEM;
    }
 
-   entry = PDE_DATA(inode);
+   entry = pde_data(inode);
 
    p->callback = entry->callback;
    if (entry->init_callback)
@@ -1866,7 +1866,7 @@ static int TAPI_seq_single_show(struct s
 #ifdef TAPI_FEAT_PROCFS
 static int TAPI_proc_single_open(struct inode *inode, struct file *file)
 {
-   return single_open(file, TAPI_seq_single_show, PDE_DATA(inode));
+   return single_open(file, TAPI_seq_single_show, pde_data(inode));
 }
 #endif /* TAPI_FEAT_PROCFS */
 
@@ -1877,24 +1877,23 @@ static void TAPI_proc_entry_create(struc
 {
    mode_t mode = S_IFREG | S_IRUGO;
 
-   memset(&proc_entry->ops, 0, sizeof(struct file_operations));
-   proc_entry->ops.owner = THIS_MODULE;
+   memset(&proc_entry->ops, 0, sizeof(struct proc_ops));
 
    if (proc_entry->single_callback)
    {
-      proc_entry->ops.open = TAPI_proc_single_open;
-      proc_entry->ops.release = single_release;
+      proc_entry->ops.proc_open = TAPI_proc_single_open;
+      proc_entry->ops.proc_release = single_release;
    } else
    {
-      proc_entry->ops.open = TAPI_proc_open;
-      proc_entry->ops.release = TAPI_proc_release;
+      proc_entry->ops.proc_open = TAPI_proc_open;
+      proc_entry->ops.proc_release = TAPI_proc_release;
    }
 
-   proc_entry->ops.read = seq_read;
-   proc_entry->ops.write = proc_entry->write_function;
+   proc_entry->ops.proc_read = seq_read;
+   proc_entry->ops.proc_write = proc_entry->write_function;
    if (proc_entry->write_function != NULL)
       mode |= S_IWUGO;
-   proc_entry->ops.llseek = seq_lseek;
+   proc_entry->ops.proc_lseek = seq_lseek;
 
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(2,6,30))
    {
@@ -2067,7 +2066,7 @@ static int tapi_proc_ppd_wr_open(
 {
    /* Copy the pointer to the tapi device struct from the inode to the file
       private data. */
-   file->private_data = PDE_DATA(inode);
+   file->private_data = pde_data(inode);
 
    return 0;
 }
@@ -2700,7 +2699,7 @@ static IFX_void_t tapi_wq_setscheduler (
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(2,6,0))
 static IFX_int32_t tapiClassCreate(void)
 {
-   pTAPI_Class = class_create(THIS_MODULE, "tapi");
+   pTAPI_Class = class_create("tapi");
    if (IS_ERR(pTAPI_Class))
    {
       TRACE( TAPI_DRV, DBG_LEVEL_HIGH,
--- a/src/drv_tapi_ppd.c
+++ b/src/drv_tapi_ppd.c
@@ -2566,6 +2566,7 @@ IFX_void_t IFX_TAPI_PPD_proc_read(struct
      - IFX_ERROR: in case of an error
 
 */
+#ifdef TAPI_FEAT_POWER
 IFX_int32_t IFX_TAPI_PPD_EnPhoneDet(TAPI_DEV *pTapiDev)
 {
    IFX_int32_t ret = TAPI_statusOk;
@@ -2649,6 +2650,7 @@ IFX_int32_t IFX_TAPI_PPD_DisPhoneDet(TAP
 #endif /* TAPI_FEAT_PHONE_DETECTION */
    return ret;
 }
+#endif
 
 
 #ifdef TAPI_FEAT_PHONE_DETECTION
