--- a/hif/pcie/8864/tx.c
+++ b/hif/pcie/8864/tx.c
@@ -719,9 +719,9 @@ void pcie_8864_tx_xmit(struct ieee80211_
 
 		if (unlikely(ieee80211_is_action(wh->frame_control) &&
 			     mgmt->u.action.category == WLAN_CATEGORY_BACK &&
-			     mgmt->u.action.u.addba_req.action_code ==
+			     mgmt->u.action.action_code ==
 			     WLAN_ACTION_ADDBA_REQ)) {
-			capab = le16_to_cpu(mgmt->u.action.u.addba_req.capab);
+			capab = le16_to_cpu(mgmt->u.action.addba_req.capab);
 			tid = (capab & IEEE80211_ADDBA_PARAM_TID_MASK) >> 2;
 			index = utils_tid_to_ac(tid);
 		}
--- a/hif/pcie/8964/tx_ndp.c
+++ b/hif/pcie/8964/tx_ndp.c
@@ -551,9 +551,9 @@ void pcie_tx_xmit_ndp(struct ieee80211_h
 
 		if (unlikely(ieee80211_is_action(wh->frame_control) &&
 			     mgmt->u.action.category == WLAN_CATEGORY_BACK &&
-			     mgmt->u.action.u.addba_req.action_code ==
+			     mgmt->u.action.action_code ==
 			     WLAN_ACTION_ADDBA_REQ)) {
-			capab = le16_to_cpu(mgmt->u.action.u.addba_req.capab);
+			capab = le16_to_cpu(mgmt->u.action.addba_req.capab);
 			tid = (capab & IEEE80211_ADDBA_PARAM_TID_MASK) >> 2;
 			index = utils_tid_to_ac(tid);
 		}
--- a/hif/pcie/8997/tx.c
+++ b/hif/pcie/8997/tx.c
@@ -671,9 +671,9 @@ void pcie_8997_tx_xmit(struct ieee80211_
 
 		if (unlikely(ieee80211_is_action(wh->frame_control) &&
 			     mgmt->u.action.category == WLAN_CATEGORY_BACK &&
-			     mgmt->u.action.u.addba_req.action_code ==
+			     mgmt->u.action.action_code ==
 			     WLAN_ACTION_ADDBA_REQ)) {
-			capab = le16_to_cpu(mgmt->u.action.u.addba_req.capab);
+			capab = le16_to_cpu(mgmt->u.action.addba_req.capab);
 			tid = (capab & IEEE80211_ADDBA_PARAM_TID_MASK) >> 2;
 			index = utils_tid_to_ac(tid);
 		}
