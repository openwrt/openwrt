/*
 * compat shim for "linux/libcompileoption.h"
 * Vendor SDK build-time feature switches.  None are needed for the GPON
 * compile milestone; the active feature defines come from the Kbuild ccflags.
 */
#ifndef _COMPAT_LINUX_LIBCOMPILEOPTION_H_
#define _COMPAT_LINUX_LIBCOMPILEOPTION_H_
#endif
