#!/bin/sh

CFG=$1

[ -n "$CFG" ] || CFG=/etc/board.json

if [ -d "/etc/board.d/" ] && [ ! -s "$CFG" ]; then
	for a in $(ls /etc/board.d/*); do
		[ -s "$a" ] || continue
		(. "$a")
	done
fi

[ -s "$CFG" ] || return 1
